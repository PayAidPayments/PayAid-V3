/** Passthrough; shell comes from app/marketing/[tenantId]/layout.tsx */
export default function MarketingSocialListeningLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
