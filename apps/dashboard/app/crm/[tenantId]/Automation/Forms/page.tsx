'use client'

import { AutomationPlaceholderPage } from '@/components/crm/automation/AutomationPlaceholderPage'

export default function AutomationFormsPage() {
  return (
    <AutomationPlaceholderPage
      title="Forms"
      description="Map each PayAid form to a destination pipeline, standardized source tags, assignment rules, acknowledgments, thank-you and scheduling behavior, field mapping, and spam controls."
    />
  )
}
