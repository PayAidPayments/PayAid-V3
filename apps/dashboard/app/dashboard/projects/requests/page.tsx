'use client'

import { P3ProjectRequestsPanel } from '@/components/projects/P3ProjectRequestsPanel'

/**
 * Thin Projects delivery operator surface.
 * Alias of the authorized P3 slice loop — not a full PM hub rebuild.
 */
export default function ProjectsDeliveryRequestsPage() {
  return <P3ProjectRequestsPanel />
}
