'use client'

/** Shell is provided by app/crm/[tenantId]/layout.tsx — avoid dual header. */
export default function CRMSalesAutomationLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
