/**
 * Entry point for ui-scanner (5 min timeout, skips tenant/dashboard/API link GETs).
 * Run: npm run test:e2e:ui-scanner  or  npx playwright test tests/ui-scanner-run.spec.ts
 */
import './ui-scanner-impl';
