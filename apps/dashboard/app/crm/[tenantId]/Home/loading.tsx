import { CRMDashboardSkeleton } from './CRMDashboardSkeleton'

/** Instant feedback while the CRM Home segment loads (RSC boundary + navigation). */
export default function CRMHomeLoading() {
  return <CRMDashboardSkeleton />
}
