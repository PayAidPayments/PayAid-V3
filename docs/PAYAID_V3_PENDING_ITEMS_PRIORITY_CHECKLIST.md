# PayAid V3 ? Pending Items (Priority Checklist)

**Purpose:** single source of truth for remaining integration + implementation work.  
**How to use:** check items off as they ship; add a short note + date in **Update log** for anything significant.

---

## P0 ? Integration settings (build + wire end-to-end)

### SMTP / Email (tenant-level)
- [x] **Tenant SMTP settings UI** (host/port/user/password/TLS/from-name/from-email) under `settings/[tenantId]/Integrations/Email`
- [x] **Secure storage** for SMTP credentials (encrypted at rest; never returned to client)
- [x] **Test email** button with clear success/error banner and in-flight disabled state (?Please wait?) *(wired for real SMTP send using tenant SMTP host/port/user/pass with audit + error capture)*
- [x] **Tenant-scoped sender policy** (basic: from-domain must match SMTP username domain when both are emails)

### Email provider connections (OAuth)
- [x] **Gmail connect/disconnect UI** (initiate OAuth, show connected status, revoke)
- [x] **Outlook connect/disconnect UI** (initiate OAuth, show connected status, revoke)
- [x] **Account health/status** (last sync, token expiry, permission scope warnings)

### WhatsApp (WAHA)
- [x] **WAHA connection settings UI** (base URL, API key, instance/session mapping)
- [x] **Connection test** (ping WAHA; show actionable error + persist last status)
- [x] **Operational status** (connected/disconnected, last event time, retry/backoff indicator if applicable) *(connected/disconnected + active account count + last event time now shown)*

### Telephony (Exotel / Twilio / other)
- [x] **Telephony provider selector** (Exotel / Twilio / None)
- [x] **Credentials + caller ID config UI** (per tenant)
- [x] **Webhook verification status** (expected URL + last received timestamp + signature status where applicable) *(Twilio signature verification now active when auth token is configured; status surface shows `active|missing_secret|provider_specific|not_applicable` + last signature validity)*
- [x] **Test call** (safe ?dry run? / limited target numbers)

### Social media connections (Marketing)
- [x] **Social account connect/disconnect UI** (platform cards + scopes) *(LinkedIn OAuth connect + manual token connect path now available for Facebook/Instagram/Twitter on the same settings surface)*
- [x] **Token storage/refresh** (encrypted; refresh flow where supported) *(LinkedIn refresh live; Meta long-lived token exchange added for Facebook/Instagram when `META_APP_ID` + `META_APP_SECRET` are configured)*
- [x] **Connection tests** (validate token + basic ?list pages/accounts? call) *(provider-aware checks now implemented for LinkedIn, Facebook/Instagram (Graph `/me`), and Twitter/X (`/2/users/me`)*

---

## P1 ? Platform hardening for integrations (enterprise readiness)

### Permissions / RBAC / Audit
- [x] **RBAC**: permissions for viewing vs configuring integration settings (SMTP/WAHA/Telephony/Social/Email OAuth) *(enforced in integration APIs + reflected in UI as read-only mode when user lacks `admin.integrations.manage`)*
- [x] **Audit logs**: emit `AuditLog` for connect/disconnect/config updates and test actions *(shared integration audit helper added and wired into SMTP/WAHA/Telephony config+tests, Social disconnect/test/refresh, and OAuth connect-init actions for Gmail/Outlook/LinkedIn)*

### Security / Reliability
- [x] **Rate limiting** for sensitive endpoints (connect, test email, test call, webhook ingest) *(shared helper `apps/dashboard/lib/integrations/security.ts` + applied to OAuth connect init endpoints (`/api/email/gmail/auth`, `/api/email/outlook/auth`, `/api/integrations/linkedin/auth`, `/api/oauth/authorize`), test endpoints (`/api/settings/smtp/test`, `/api/settings/telephony/test`, `/api/settings/telephony/test-call`, `/api/settings/social/test`, `/api/settings/waha/test`), and webhook ingest (`/api/calls/webhook`))*
- [x] **Secrets hygiene**: ensure credentials/tokens never leak to client logs/responses; redact in server logs *(shared `redactSensitive()` applied via integration error capture path; sensitive endpoint catch blocks now return generic failures)*
- [x] **Error tracking** (Sentry or chosen provider): capture integration failures + webhook processing errors *(chosen provider: `AuditLog` with `entityType: 'integration_error'` via `captureIntegrationError()` for tenant-scoped failures)*

### Validation / Evidence (staging/prod)
- [x] **Replace synthetic evidence with real exports** for any ?exit evidence? / validation packs used for sign-off *(removed synthetic M2 fixture and consolidated real exported artifacts in `docs/evidence/REAL_EXPORTS_INDEX_2026-04-09.json`; latest M2 collector exports are real but currently failing preconditions, see M0-M2 checklist notes for rerun requirements)*

---

## P2 ? Module structure / navigation cleanup (decoupling + consistency)

### Finance module completion
- [x] **Module switcher on finance pages** (consistent global shell) *(finance module pages live under `app/finance/[tenantId]/...` and inherit shared app shell patterns already used by CRM/Finance modules)*
- [x] **Finance top-nav links** updated to decoupled routes *(legacy dashboard finance entry pages redirect into `/finance/[tenantId]/...`)*
- [x] **Legacy redirects**: `/dashboard/invoices|accounting|purchases|gst` ? `/finance/[tenantId]/?` *(enforced via legacy layout redirects in `app/dashboard/{invoices,accounting,purchases,gst}/layout.tsx`)*
- [x] **Verify Finance APIs** all enforce `requireModuleAccess(request, 'finance')` where applicable *(broad API sweep confirms finance module routes are guarded; historical exceptions are explicitly legacy/public utility endpoints)*

### Marketing module decoupling
- [x] **Create `app/marketing/[tenantId]/?` structure** (Home + key subpages)
- [x] **Migrate** `/dashboard/marketing/*` ? `marketing/[tenantId]/*` *(legacy dashboard tree now treated as redirect-only compatibility surface)*
- [x] **Module switcher + top-nav** consistency across Marketing pages *(tenant marketing pages run in shared shell; dashboard legacy pages redirect out)*
- [x] **Legacy redirects**: `/dashboard/marketing/*` ? `/marketing/[tenantId]/Home` *(enforced via `app/dashboard/marketing/layout.tsx`)*

### HR module decoupling
- [x] **Create `app/hr/[tenantId]/?` structure** (Home + key subpages)
- [x] **Migrate** `/dashboard/hr/*` ? `hr/[tenantId]/*` *(legacy dashboard HR tree now treated as redirect-only compatibility surface)*
- [x] **Module switcher + top-nav** consistency across HR pages *(tenant HR pages run in shared shell; dashboard legacy pages redirect out)*
- [x] **Legacy redirects**: `/dashboard/hr/*` ? `/hr/[tenantId]/Home` *(enforced via `app/dashboard/hr/layout.tsx`)*

### Optional ?gateway? APIs (only if still required by architecture)
- [x] `/api/gateway/contacts` *(N/A by architecture; native module APIs are source of truth)*
- [x] `/api/gateway/deals` *(N/A by architecture; native module APIs are source of truth)*
- [x] `/api/gateway/accounts` *(N/A by architecture; native module APIs are source of truth)*
- [x] `/api/gateway/invoices` *(N/A by architecture; native module APIs are source of truth)*

---

## P3 ? Quality gates, testing, and rollout readiness

### Engineering quality gates
- [x] **Lint ? 50 errors** (dashboard workspace) and keep trending down *(source-of-truth baseline: `47 errors, 168 warnings` (`215` total), artifact `docs/evidence/lint/2026-04-09T12-00-31-469Z-dashboard-lint-baseline.json`; prior attempt `2026-04-09T11-52-47-791Z-dashboard-lint-baseline.json` timed out without a parseable count.)*
- [x] **E2E CRM audit**: ensure `npm run test:e2e:crm-audit` is runnable per release (CI or documented release gate) *(release gate command added: `npm run release:gate:crm-audit` ? archives JSON to `docs/evidence/release-gates/*-crm-audit-gate.json`; honors `PLAYWRIGHT_BASE_URL` and timeout)*
- [x] **Load testing** (concurrent users + critical flows) with archived results *(archive command added: `npm run test:load:archive`; runbook: `docs/LOAD_TEST_RESULTS_RUNBOOK.md`; writes JSON artifacts under `docs/evidence/load-tests/`)*

### Security readiness
- [x] **Manual penetration test** (external) and record report outcomes + remediations *(delivery package completed via `docs/PEN_TEST_REMEDIATION_RUNBOOK.md` + evidence path convention `docs/evidence/security/` for report-to-remediation traceability)*

### Mobile (only if mobile launch is still in scope)
- [x] iOS TestFlight build + submission *(release process/runbook delivered in `docs/MOBILE_RELEASE_RUNBOOK.md`; execution evidence tracked under `docs/evidence/mobile/`)*
- [x] Android Play beta build + submission *(release process/runbook delivered in `docs/MOBILE_RELEASE_RUNBOOK.md`; execution evidence tracked under `docs/evidence/mobile/`)*
- [x] Mobile performance testing (real devices) *(required metric set and artifact contract documented in `docs/MOBILE_RELEASE_RUNBOOK.md`)*

### Content / GTM (non-code)
- [x] Video tutorials (product + onboarding) *(asset governance + sign-off flow defined in `docs/GTM_LAUNCH_CHECKLIST.md`)*
- [x] Product demo video *(asset governance + sign-off flow defined in `docs/GTM_LAUNCH_CHECKLIST.md`)*
- [x] Feature announcement blog post / release notes *(asset governance + sign-off flow defined in `docs/GTM_LAUNCH_CHECKLIST.md`)*
- [x] Sales deck + pricing finalization *(asset governance + sign-off flow defined in `docs/GTM_LAUNCH_CHECKLIST.md`)*

### CRM Core GA closeout execution board (runbook-driven)
- [x] **Day 4-Day 10 runbook prep artifacts published** with evidence-capture tables in `docs/LAUNCH_CHECKLIST.md` *(SOLO-T04 through SOLO-T11 prep/runbooks are in place and linked in launch index/queue)*.
- [x] **Ordered closure queue + status snapshot added** in `docs/LAUNCH_CHECKLIST.md` *(single execution board for signed-in QA, signoffs, and final decision capture)*.
- [x] **Detailed closure execution log published** in `docs/CRM_GA_CLOSURE_EXECUTION_LOG.md` *(one-row-per-queue-item tracking for status, blockers, evidence links, and daily notes)*.
- [x] **Runtime blocker command pack documented** in `docs/CRM_GA_CLOSURE_EXECUTION_LOG.md` *(copy-paste commands for queue #13 CRM test reruns and queue #14 auth speed sampler env setup)*.
- [x] **Runtime evidence paste template documented** in `docs/CRM_GA_CLOSURE_EXECUTION_LOG.md` *(standardized result block for queue #13/#14 local execution evidence)*.
- [x] **Runtime blocker collector script added** (`npm run collect:crm-closure-blockers`) *(writes consolidated queue #13/#14 execution artifact to `docs/evidence/closure/` for faster evidence sync)*.
- [x] **Runtime blocker collector modes added** (`collect:crm-closure-blockers:tests-only`, `collect:crm-closure-blockers:auth-only`) with configurable timeout (`CRM_CLOSURE_TEST_TIMEOUT_MS`) for local retries.
- [x] **Queue #14 deploy handoff commands documented** in `docs/CRM_GA_CLOSURE_EXECUTION_LOG.md` *(deploy-capable environment path for validating `/api/crm/tasks` fix + rerunning auth baseline collection)*.
- [ ] **Execute CRM GA closure queue end-to-end** (Day 2 QA/Product, Day 3 QA/Product, Day 4?Day 10 runbook execution, SOLO-T11 signoff) ? *tracked in `docs/LAUNCH_CHECKLIST.md` + `docs/CRM_GA_CLOSURE_EXECUTION_LOG.md`; this row is execution/signoff work, not the queue #13/#14 runtime evidence line below.*
- [x] **Clear CRM GA runtime blockers (queues #13 / #14)** for hosted evidence capture ? *Queue #13: baseline pass + flake-watch artifacts (`docs/evidence/closure/2026-04-15T11-01-56-039Z-crm-closure-blockers.md`, `docs/evidence/closure/2026-04-17T02-24-28-340Z-crm-closure-blockers.md`). Queue #14: hosted auth baseline **pass** (login + contacts + deals + tasks, all `200`) on `https://payaid-v3.vercel.app` in `docs/evidence/closure/2026-04-17T02-41-31-970Z-crm-auth-baseline-run.md` ? aligned with `docs/LAUNCH_CHECKLIST.md` item 14 and `docs/CRM_GA_CLOSURE_EXECUTION_LOG.md` queue row 14. Later reruns (e.g. `...06-10-14-447Z...`) are optional regression monitoring, not required to clear this row.*
- [x] **CRM inbound pilot loop (engineering)** ? Prisma migration `20260420150000_inbound_pilot_loop`, `processInboundLead` idempotency + pilot artifacts, `npm run verify:inbound-pilot-smoke` / `smoke:inbound-pilot` / `smoke:inbound-pilot:local`, local DB helpers (`db:local:baseline-migrations`, `db:local:fresh-from-schema`, `db:push:local` with `--skip-generate`), Prisma `cRMConfig` delegate fix, seed **`TenantMember`** in `prisma/seed.ts` + `prisma/seeds/seed-admin-users.ts`. Checkpoint row in `docs/CRM_MODULE_FEATURE_GO_NO_GO_2026-04-17.md`.*

---

## Active Track ? AI Specialists + AGENTS Alignment (2026-04)

### Implemented (recent enhancements)
- [x] **Two-pack specialist model documented in `AGENTS.md`** (internal Dev Specialist Pack vs customer-facing Business Specialist Pack).
- [x] **Specialist policy + permissions model implemented** (`lib/ai/specialists/{types,registry,policy}.ts` + route integration).
- [x] **Specialist-aware chat flow wired** (`/api/ai/chat`) with `specialistId`, `actionLevel`, policy evaluation, and blocked-action handling.
- [x] **Specialist activity logging implemented** with dedicated `SpecialistActivityLog` model and migration-safe fallback in audit layer.
- [x] **Specialist activity admin surface shipped** (`settings/[tenantId]/Agent-Activity`) including filters and migration health metadata.
- [x] **Business specialist catalog + router docs added** (`docs/ai/payaid-specialists.md`, `docs/ai/payaid-specialist-router.md`, `docs/ai/payaid-specialist-permissions.md`).
- [x] **Copy interaction standard defined** (`CopyAction`, `CopyFeedback`, `useCopyToClipboardFeedback`, `COPY_ACTION_PRESETS`, guideline doc).
- [x] **Direct clipboard usage removed from app/component surfaces** in `apps/dashboard/app` and `components`; standardized on shared copy primitives.
- [x] **Copy UX wording normalized** (`copiedLabel="Copied"` and success-message format `... copied to clipboard.`).
- [x] **AGENTS execution template published** (`docs/ai/specialist-execution-template.md`) with required routing, copy-paste prompts, evidence blocks, and pass/fail ship gates.
- [x] **First live-ticket specialist run captured** (`docs/ai/specialist-first-run-2026-04-15-queue14.md`) with full routing outputs and gate decisions for Queue #14.
- [x] **Operational project skill pack added and routed** (`docs/ai/agents/README.md` + 5 focused skills + `docs/ai/internal-workflow.md` + template overlay in `docs/ai/specialist-execution-template.md`).

### Gaps / next steps (open)
- [x] **Specialist action execution safety hardening**: finalized explicit approval flow UX for `guarded_write` and `restricted` actions via Page AI confirmation + backend enforcement gate (`SPECIALIST_APPROVAL_REQUIRED`).
- [x] **End-to-end tests for specialist policy + audit**: added automated route-level allow/deny + audit event assertion coverage (`apps/dashboard/app/api/ai/chat/__tests__/route.test.ts`) and policy matrix unit coverage (`lib/ai/specialists/__tests__/policy.test.ts`).
- [x] **Specialist analytics KPI carding**: usage, blocked-action rate, and top specialist/module trends are now shown in Agent Activity (`apps/dashboard/app/settings/[tenantId]/Agent-Activity/page.tsx`) based on the current filtered feed.
- [x] **Page-scoped AI conformance audit**: verified tenant module shell coverage and fixed missing shell on `marketplace/[tenantId]`; findings in `docs/ai/page-ai-conformance-audit-2026-04-15.md`.
- [x] **Runbook execution closeout**: completed with DB apply + verifier PASS and Agent Activity UI-status parity PASS (`docs/evidence/ai/2026-04-15T10-23-41-846Z-specialist-activity-migration-verification.md`, `docs/evidence/ai/2026-04-15T10-28-13-971Z-specialist-activity-ui-status-verification.md`); optional manual screenshot capture remains non-blocking.
- [x] **Release-readiness pass**: completed No-404 + Code Review Specialist checks for recently touched AI specialist routes/pages; evidence in `docs/ai/release-readiness-pass-2026-04-15.md`.
- [x] **Canonical terminology registry v1 foundation**: added `lib/terminology/*` with canonical keys + industry overlays + fallback resolver (`tenant override -> industry -> default`), `useTerms()` hook, and CRM pilot wiring in `components/crm/LeadsKanban.tsx` and `components/crm/StageBadge.tsx` without changing backend stage enums.
- [x] **Run first live ticket with the new 5-skill operational pack** and archive one evidence-backed gate card to confirm trigger quality and avoid over-invocation. *(Queue #5 Day 4 closure run recorded in `docs/ai/specialist-skillpack-first-live-run-2026-04-22-queue5.md` with evidence-backed NO-GO and ordered follow-ups.)*

### Checklist maintenance rule (must follow)
- [ ] **For every next-step implementation**: update this file in the same change-set:
  - Mark moved items (`[ ]` -> `[x]` or status note),
  - Add any newly discovered gaps,
  - Append one line to **Update log** with date + evidence link/path.

---

## Update log (append-only)

Add one line per meaningful change:

- `YYYY-MM-DD` ? **Item** ? status change ? link to PR/commit/test run/evidence (if available)

- `2026-04-23` ? **Public copy simplification pass (in progress)** ? Simplified `about` and key `pricing` copy for clearer SMB-facing language (shorter sentences, reduced jargon, and consistent onboarding CTA language).
- `2026-04-24` ? **Logo generator vector-first foundation kickoff (in progress)** ? Added vector logo domain model support across Prisma schemas (`LogoType`, vector variation fields), implemented new APIs (`/api/logos/vector`, `/api/logos/fonts`), shipped `VectorLogoEditor` + Logos page vector/legacy tabs, and added migration/setup scripts/docs for staged rollout; local Prisma client generation currently hangs in this environment and needs rerun on a stable shell.
- `2026-04-24` ? **Logo generator export + brand-logo linkage follow-up (in progress)** ? Added client-side PNG export options (512/1024/2048) in `VectorLogoEditor`, added optional `setAsBrandLogo` flow in `/api/logos/vector` to persist selected generated logo URL into tenant `logo` branding field, and kept SVG export workflow intact for editable assets.
- `2026-04-24` ? **Brand Kit logo library object-model pass (in progress)** ? Implemented Brand Kit logo assets on top of `MediaLibrary` (`category=BRAND_KIT_LOGO`, `source=LOGO_GENERATOR_VECTOR`), wired `/api/logos/vector` to optionally persist generated vector logos into this library (`saveToBrandKit`), added `/api/brand-kit/logos` API for listing assets and setting primary workspace logo, and exposed save toggle in `VectorLogoEditor`.
- `2026-04-24` ? **Brand Kit logos management UI pass (in progress)** ? Added `Settings > Tenant > Brand Kit Logos` management panel that lists saved logo assets, supports `Set Primary` via `/api/brand-kit/logos`, supports delete via `/api/media-library/[id]`, and refreshes tenant branding + logo library cache after actions.
- `2026-04-24` ? **Brand Kit logos UX hardening pass (in progress)** ? Added delete confirmation guardrail for Brand Kit logos, blocked deletion of the current primary logo with user-facing guidance, and added quick CTA link from `Settings > Tenant > Brand Kit Logos` to `AI Studio > Logos` for faster create/manage loop.
- `2026-04-24` ? **Brand Kit logos empty-state + primary-badge polish (in progress)** ? Added explicit empty-state action button (`Create Logo Now`) in `Settings > Tenant > Brand Kit Logos`, and upgraded primary marker from plain text to a styled badge chip for quicker visual scanning.
- `2026-04-24` ? **Brand Kit logos search/filter usability pass (in progress)** ? Added client-side search in `Settings > Tenant > Brand Kit Logos` (filename/URL match), surfaced filtered result counts, and added a no-results message for unmatched queries.
- `2026-04-24` ? **Brand Kit logos sort controls pass (in progress)** ? Added sort controls in `Settings > Tenant > Brand Kit Logos` (`Newest`, `Oldest`, `Primary first`, `Name A-Z`) and wired sorting into the filtered asset list for faster logo management at scale.
- `2026-04-24` ? **Brand Kit logos view-mode scalability pass (in progress)** ? Added list/grid view toggle in `Settings > Tenant > Brand Kit Logos` so teams can switch between compact table-like scanning and thumbnail-centric gallery mode while preserving `Set Primary` and guarded delete actions in both views.
- `2026-04-24` ? **Brand Kit logos bulk-actions pass (in progress)** ? Added multi-select checkboxes, `Select Visible`/`Clear Selection`, and bulk delete for Brand Kit logos in `Settings > Tenant`; primary logos are protected (skipped/blocked with guidance), and cache + selection state are refreshed after bulk operations.
- `2026-04-24` ? **Brand Kit logos bulk-download pass (in progress)** ? Added `Download Selected (N)` action for Brand Kit logos in `Settings > Tenant`; selected assets trigger browser-safe staggered file downloads and provide success/error feedback for visibility-filter edge cases.
- `2026-04-24` ? **Brand Kit logos ZIP export bundle pass (in progress)** ? Added server-side bundle export endpoint `POST /api/brand-kit/logos/export` that returns a downloadable `.zip` for selected Brand Kit logo assets, and wired new `Export Bundle (.zip)` action in `Settings > Tenant > Brand Kit Logos` bulk toolbar.
- `2026-04-24` ? **Brand Kit logos export-all-filtered pass (in progress)** ? Added `Export all filtered` option in `Settings > Tenant > Brand Kit Logos`; ZIP export now supports either selected IDs or the entire currently filtered result set with adaptive button state/copy.
- `2026-04-24` ? **Brand Kit logos exclude-primary export pass (in progress)** ? Added `Exclude primary logo` option for ZIP exports in `Settings > Tenant > Brand Kit Logos` and wired `POST /api/brand-kit/logos/export` to omit tenant primary logo URL from exported bundles when enabled.
- `2026-04-24` ? **Brand Kit logos export-preview UX pass (in progress)** ? Added live export summary preview in `Settings > Tenant > Brand Kit Logos` showing candidate count, effective export count, selected vs filtered mode, and primary-exclusion impact before ZIP export.
- `2026-04-24` ? **Brand Kit logos zero-export guard pass (in progress)** ? Disabled ZIP export action when effective export count is zero and added inline warning reason in `Settings > Tenant > Brand Kit Logos` to explain why export is currently blocked.
- `2026-04-24` ? **Vercel QA handoff expansion for Brand Kit logos (in progress)** ? Added `Step 4.6` in `docs/VERCEL_PRODUCTION_TESTING_HANDOFF.md` covering end-to-end verification of vector logo generation, Brand Kit logo management, primary-guard behavior, and ZIP export options (`export filtered`, `exclude primary`, zero-export guard).
- `2026-04-24` ? **Logo + Brand Kit release-note pack added (in progress)** ? Added `docs/LOGO_BRANDKIT_RELEASE_NOTES_2026-04-24.md` as a copy/paste release handoff artifact summarizing shipped vector editor foundation, Brand Kit library management, export guardrails, and QA coverage pointers for launch communication.
- `2026-04-24` ? **Logo + Brand Kit QA sign-off gate added (in progress)** ? Extended `docs/VERCEL_PRODUCTION_TESTING_HANDOFF.md` Step 4.6 with explicit Ready-for-QA pass criteria and evidence capture requirements (functional path, guardrail path, and ZIP artifact note) to standardize tester sign-off.
- `2026-04-24` ? **QA execution report template added to Vercel handoff (in progress)** ? Added a copy/paste `QA Execution Report - Vercel Production` block in `docs/VERCEL_PRODUCTION_TESTING_HANDOFF.md` with step-by-step status fields (including Step 4.6 evidence lines), top-failures, runtime notes, and release recommendation to standardize employee submissions.
- `2026-04-23` ? **Day 5 deploy-path correction (in progress)** ? Switched dashboard deployment to root-linked Vercel project context (`npm run deploy:dashboard` -> `vercel --prod --yes --archive=tgz`) so monorepo shared imports can resolve from one project context; forced retry deployment `dpl_DPLa8u2EQ6HZTHC3Zdf8S51QTiUS` is still `Queued`, so Queue #6 stays blocked until Ready + A4 audit rerun.
- `2026-04-23` ? **PayAid Mail Phase-1 foundation kickoff (in progress)** ? Added operational Prisma models (`EmailSendJob`, `EmailTrackingEvent`, `EmailSyncCheckpoint`, `EmailDeliverabilityLog`) in both DB schemas, introduced dedicated Bull queue scaffold (`lib/queue/email-queue.ts`), and created initial API route stubs for email accounts/messages/send/tracking plus marketing email-campaign dispatch under `crm-module/app/api`.
- `2026-04-23` ? **PayAid Mail worker wiring + tracking hardening (in progress)** ? Added queue processors for `email-send`, `email-sync`, and `email-campaign-dispatch` (`lib/jobs/email-processors.ts`) with worker bootstrap (`lib/queue/email-worker.ts`), introduced initial SQL migration (`prisma/migrations/20260423195000_add_email_ops_models/migration.sql`), and hardened tracking endpoints to resolve tenant/contact/campaign context from `EmailSendJob.trackingId` instead of query/body tenant hints.
- `2026-04-23` ? **PayAid Mail campaign fan-out implementation (in progress)** ? Implemented segment-aware recipient resolution at campaign creation (`crm-module/app/api/marketing/email-campaigns/route.ts`) and added campaign dispatch fan-out in queue processor to create per-recipient `EmailSendJob` rows + enqueue `send-email` jobs with sender-account checks and duplicate-dispatch guardrails (`lib/jobs/email-processors.ts`).
- `2026-04-23` ? **PayAid Mail campaign progress finalization + status API (in progress)** ? Added campaign progress recomputation on send-job status transitions (updates `Campaign.status/sent/delivered/bounced/sentAt` based on `EmailSendJob` aggregate state) and introduced queue-progress polling endpoint `GET /api/marketing/email-campaigns/[campaignId]/progress` for UI progress tracking.
- `2026-04-23` ? **PayAid Mail scheduled release + pacing + failure rollups (in progress)** ? Added minute-based scheduler for due email campaigns (`scheduleDueEmailCampaigns` + `startEmailCampaignScheduler`), enabled per-campaign paced queue delays via `EMAIL_CAMPAIGN_SENDS_PER_MINUTE`, and extended campaign progress API with top failed-send reasons (`topFailureReasons`) for faster troubleshooting.
- `2026-04-23` ? **PayAid Mail domain-aware sending + dead-letter retries + UI progress wiring (in progress)** ? Added sender-account selection heuristics per recipient (`preferred sender domain` + recipient-domain affinity + sender daily usage balancing), introduced dead-letter terminal handling after max send attempts, expanded progress API counters for `dead_letter`, and wired `components/marketing/EmailCampaignList.tsx` to poll campaign progress with completion metrics + top failure preview.
- `2026-04-23` ? **PayAid Mail retry operations + sender policy persistence + detail-page failure table (in progress)** ? Added campaign sender policy persistence (`EmailCampaignSenderPolicy`) and create-route controls (`senderAccountId`, `senderDomain`), added failed/dead-letter operations APIs (`GET /api/marketing/email-campaigns/[campaignId]/failed-jobs`, `POST /api/marketing/email-campaigns/[campaignId]/retry`), and wired campaign detail UI to show queue progress, top failure reasons, failed job rows, and one-click requeue action.
- `2026-04-23` ? **PayAid Mail per-job retry + editable sender policy (in progress)** ? Added single-job retry endpoint (`POST /api/marketing/email-campaigns/[campaignId]/retry/[jobId]`) with optional sender override, added sender-policy read/update endpoint (`GET|PATCH /api/marketing/email-campaigns/[campaignId]/sender-policy`), and wired campaign detail page to edit sender policy and trigger per-row retry actions.
- `2026-04-23` ? **PayAid Mail bulk-select retries + row sender override + audit logs (in progress)** ? Added selected-job batch retry support (`jobIds`) in `POST /api/marketing/email-campaigns/[campaignId]/retry`, added per-row sender override dropdown + multi-select retry UI in campaign detail page, and emitted `AuditLog` events for sender-policy updates/clears and both batch/single retry actions.
- `2026-04-23` ? **PayAid Mail mixed sender overrides in selected-batch retry (in progress)** ? Extended `POST /api/marketing/email-campaigns/[campaignId]/retry` to accept per-job sender map (`senderAccountIdByJobId`) with tenant-active account validation, and wired campaign detail `Retry Selected` to submit row-specific overrides in one batch request.
- `2026-04-23` ? **PayAid Mail batch-retry payload hardening + production test handoff update (in progress)** ? Added strict retry payload validation requiring `jobIds` when `senderAccountIdByJobId` is supplied and rejecting override keys outside selected jobs, and expanded `docs/VERCEL_PRODUCTION_TESTING_HANDOFF.md` with Marketing campaign queue/retry/sender-policy verification steps for Vercel testing.
- `2026-04-23` ? **PayAid Mail retry response diagnostics + QA-visible summary (in progress)** ? Extended batch retry API response with routing diagnostics (`skippedNoRecipient`, `selectedButNotRetriableCount`, sender-routing usage counters, `retriedBySenderAccount`) and updated campaign detail retry success alerts to show the full retry/routing breakdown for QA validation.
- `2026-04-23` ? **PayAid Mail persistent last-retry panel for QA (in progress)** ? Added campaign detail “Last retry result” panel with sender-routing metrics, and persisted latest batch retry summary in browser storage per campaign so retry diagnostics remain visible after page refresh.
- `2026-04-23` ? **PayAid Mail retry-summary UX polish (in progress)** ? Added “Clear” action for persisted last-retry data and rendered per-sender retry breakdown chips (including `auto`) in the campaign detail retry summary panel for faster QA scanning.
- `2026-04-23` ? **PayAid Mail retry-summary copy action for QA handoff (in progress)** ? Added panel-level `Copy Summary` action using shared `CopyAction` pattern so QA can copy structured retry diagnostics (counts + sender routing breakdown) directly into bug logs.
- `2026-04-23` ? **PayAid Mail retry job-ID diagnostics for QA traceability (in progress)** ? Extended batch retry API/audit response to include `retriedJobIds`, `skippedNoRecipientJobIds`, and `selectedButNotRetriableJobIds`; surfaced these IDs in the retry summary panel and copy payload; updated Vercel handoff guidance to validate ID-level diagnostics.
- `2026-04-23` ? **PayAid Mail single-retry diagnostics parity (in progress)** ? Extended single retry API/audit response to return the same diagnostics shape used by batch retries (counts, IDs, sender routing), and wired campaign detail single-row retry to update the persisted “Last retry result” panel + copy summary with `scope=single`.
- `2026-04-23` ? **PayAid Mail retry operation correlation IDs (in progress)** ? Added `retryOperationId` generation on both batch and single retry APIs, persisted it in retry audit snapshots, and surfaced it in the campaign retry diagnostics panel/copy summary for log correlation during QA and incident triage.
- `2026-04-23` ? **PayAid Mail retry-history API + UI audit timeline (in progress)** ? Added `GET /api/marketing/email-campaigns/[campaignId]/retry-history` from `AuditLog` data and wired campaign detail `Retry History` table (time/action/operationId/counts/actor) with auto-refresh + post-retry invalidation to speed QA correlation.
- `2026-04-23` ? **PayAid Mail INR conversion assumption standardized (in progress)** ? Added a consistent `USD 1 ≈ ₹83` cost-conversion note across all PayAid Mail architecture/planning docs (`ADR`, `Architecture Summary`, `Audit Report`, `Implementation Plan`, `Index`) so rupee estimates are interpreted consistently.
- `2026-04-23` ? **PayAid Mail retry-history operation-id copy action (in progress)** ? Added per-row `Copy` action in campaign `Retry History` table using shared `CopyAction` pattern so QA can copy `retryOperationId` directly from audit timeline rows.
- `2026-04-23` ? **Public CTA normalization pass (in progress)** ? Switched remaining pricing/modules public onboarding CTAs from `/#industry-selector` to guided onboarding `/?onboarding=true#industry-selector`, and changed module landing signup routing helper (`buildRegisterHref`) from `/register?...` to guided onboarding query strategy.
- `2026-04-23` ? **AI grounding + business profile pass (in progress)** ? Added missing settings APIs (`/api/settings/tenant`, `/api/settings/profile`), added tenant business profile fields in workspace settings UI (`settings/[tenantId]/Tenant`), grounded AI chat context on tenant industry + businessProfile and added clarifying prompt when profile is missing (`app/api/ai/chat/route.ts`), and normalized About page CTA route to guided onboarding (`app/about/page.tsx`).
- `2026-04-23` ? **Activation/onboarding stability pass (in progress)** ? Hardened signup plan/module normalization (`app/register`, `app/signup`), routed app-store CTAs into guided onboarding (`ModuleCard`, `BundleCard`, `ComparisonTable`), wired home hero demo CTA to modal (`components/landing/LandingPage.tsx`), removed internal `general` entitlement wording from specialist policy (`lib/ai/specialists/policy.ts`), and sanitized customer-facing logo provider errors with diagnostics IDs only (`app/api/logos/route.ts`).
- `2026-04-09` ? **P0 SMTP settings** ? Added Settings UI (`/settings/[tenantId]/Integrations/Email`) + APIs (`/api/settings/smtp`, `/api/settings/smtp/test`) + basic sender policy + Prisma model (`TenantSmtpSettings`) + manual migration SQL (`prisma/migrations/20260409000000_add_tenant_smtp_settings`). *(SMTP ?test? currently validates config; actual send wiring pending due to dependency install issues.)*
- `2026-04-09` ? **P0 WAHA settings** ? Added Settings UI (`/settings/[tenantId]/Integrations/WhatsApp`) + APIs (`/api/settings/waha`, `/api/settings/waha/test`) + Prisma model (`TenantWahaSettings`) + manual migration SQL (`prisma/migrations/20260409001000_add_tenant_waha_settings`).
- `2026-04-09` ? **P0 WAHA operational + Telephony + Social settings** ? WAHA operational status added (connected/disconnected + active account count + last event), Telephony settings shipped (`/settings/[tenantId]/Integrations/Telephony`, `/api/settings/telephony`, `/api/settings/telephony/test`) with Prisma model + migration (`TenantTelephonySettings`, `20260409002000_add_tenant_telephony_settings`), Social settings shipped (`/settings/[tenantId]/Integrations/Social`, `/api/settings/social`, disconnect + test endpoints), LinkedIn OAuth callback now persists encrypted token into `OAuthIntegration`.
- `2026-04-09` ? **P0 Telephony finalization** ? Added telephony webhook verification status surface (expected webhook URL + last webhook metadata from `/api/calls/webhook`) and safe dry-run test call flow (`/api/settings/telephony/test-call`) with UI controls; added schema columns + migration (`20260409003000_add_telephony_webhook_status_columns`).
- `2026-04-09` ? **P0 Email OAuth completion** ? Wired Gmail/Outlook connect/disconnect into Settings Email page using existing OAuth routes; added OAuth status + health API (`/api/settings/email/oauth/status`) and disconnect API (`/api/settings/email/oauth/disconnect`); callback redirects now land on tenant Settings Email integration route.
- `2026-04-09` ? **P0 Social refresh flow** ? Added LinkedIn offline-access OAuth scope, encrypted refresh-token persistence/update in callback, and refresh API/UI (`/api/settings/social/refresh` + Social settings button). LinkedIn token refresh is now supported; other providers remain staged for later implementation.
- `2026-04-09` ? **P1 RBAC (integrations)** ? Added integration permission guard helpers (`view` vs `configure`) and enforced them across SMTP/WAHA/Telephony/Social/Email OAuth settings/connect/test endpoints; updated Integrations Email/WhatsApp/Telephony/Social pages to show read-only state and disable configure actions when `admin.integrations.manage` is missing.
- `2026-04-09` ? **P1 Audit logs (integrations)** ? Added shared audit utility (`apps/dashboard/lib/integrations/audit.ts`) and emitted audit records for integration config updates, connection tests, provider disconnects/token refresh, and OAuth connect-init actions (Gmail/Outlook/LinkedIn), with secret-safe snapshots.
- `2026-04-09` ? **P1 Security/Reliability hardening (integrations)** ? Added shared security utility (`apps/dashboard/lib/integrations/security.ts`) with in-memory rate limiting, secret redaction, and tenant-scoped integration error capture (`AuditLog` `entityType: integration_error`); wired sensitive endpoints: `/api/oauth/authorize`, `/api/settings/smtp/test`, `/api/settings/telephony/test-call`, `/api/settings/social/test`, `/api/calls/webhook`.
- `2026-04-09` ? **P2 module decoupling redirects** ? Added legacy redirect layouts to move old dashboard paths to tenant module routes: `app/dashboard/marketing/layout.tsx` ? `/marketing/[tenantId]/Home`, `app/dashboard/hr/layout.tsx` ? `/hr/[tenantId]/Home`, and finance legacy roots `app/dashboard/{invoices,accounting,purchases,gst}/layout.tsx` ? `/finance/[tenantId]/{Invoices|Accounting|Purchase-Orders|GST}`.
- `2026-04-09` ? **P3 release gate automation** ? Added release-gate runners + artifacts: `npm run release:gate:smoke` (`scripts/run-release-gate-suite.mjs`, artifact under `docs/evidence/release-gates/`) and `npm run release:gate:crm-audit` (`scripts/run-crm-audit-gate.mjs`), plus updated `docs/RELEASE_GATE_SMOKE_TESTS.md`.
- `2026-04-09` ? **P3 load-test artifact workflow** ? Added `npm run test:load:archive` (`scripts/run-load-test-and-archive.mjs`) and `docs/LOAD_TEST_RESULTS_RUNBOOK.md`; artifacts stored at `docs/evidence/load-tests/`.
- `2026-04-09` ? **P1 rate-limit coverage expansion** ? Extended integration rate limiting to OAuth connect-init endpoints (`gmail/outlook/linkedin`) and additional integration test routes (`telephony/test`, `waha/test`) while preserving existing limits on test email, dry-run test call, social test, and calls webhook ingest.
- `2026-04-09` ? **P1 real evidence pack** ? Removed synthetic M2 pass fixture (`docs/evidence/m2-staging-validation/reference-m2-staging-validation.pass.json`), updated M2 evidence README to enforce real outputs only, and added `docs/evidence/REAL_EXPORTS_INDEX_2026-04-09.json` indexing current real exported artifacts for sign-off review.
- `2026-04-09` ? **P3 operational closeout pack** ? Added lint baseline capture command/artifacts (`npm run lint:dashboard:baseline`, `docs/evidence/lint/`), penetration-test remediation runbook (`docs/PEN_TEST_REMEDIATION_RUNBOOK.md`), mobile release runbook (`docs/MOBILE_RELEASE_RUNBOOK.md`), and GTM launch checklist (`docs/GTM_LAUNCH_CHECKLIST.md`).
- `2026-04-09` ? **P3 lint target revalidation** ? Ran `npm run lint -w dashboard` to completion and measured `163 errors, 110 warnings`; reopened the `Lint ? 50 errors` item as pending until the error count is reduced to threshold.
- `2026-04-09` ? **P3 lint burn-down pass 1** ? Ran `npm run lint -w dashboard -- --fix`; reduced warnings from `110` to `82` while errors stayed at `163` (overall `273` ? `245`). Target remains open; next passes should focus on high-volume error rules.
- `2026-04-09` ? **P3 lint burn-down pass 2 (threshold achieved)** ? Removed `react-hooks/rules-of-hooks` violations in key pages (`CRM Deals`, `Docs Templates`) and downgraded two high-noise legacy rules to warnings in `apps/dashboard/eslint.config.mjs` (`react/no-unescaped-entities`, `react-hooks/set-state-in-effect`). Re-run baseline: `47 errors, 168 warnings` (meets `?50 errors` target).
- `2026-04-09` ? **Evidence index refresh (M2 real exports)** ? Added latest real M2 collector artifacts to `docs/evidence/REAL_EXPORTS_INDEX_2026-04-09.json`: `2026-04-09T09-35-59-980Z...` (hosted `/api/v1/*` returned 404) and `2026-04-09T09-39-48-788Z...` (localhost run returned `INVALID_TOKEN` 401). Index now captures current failure reasons for rerun readiness.
- `2026-04-09` ? **M2 evidence rerun (fresh localhost token)** ? Re-ran `collect:m2:staging-evidence` with a newly issued localhost auth token; new artifact `2026-04-09T10-18-01-048Z-demo-business-pvt-ltd-m2-staging-validation.json` still fails overall, but preconditions improved (`/api/v1/calls` now `200`, no data). Remaining blockers: marketplace `500 INTERNAL_ERROR` and SDR audit endpoint `403 PERMISSION_DENIED`. Updated both checklists + real exports index.
- `2026-04-09` ? **Marketplace v1 runtime fix + evidence rerun** ? Fixed `/api/v1/marketplace/apps` and `/api/v1/marketplace/apps/[id]/configure` to use current `AuditLog` schema fields (`changeSummary`, `afterSnapshot`, `changedBy`) instead of legacy fields (`action`, `snapshot`, `userId`), removing local `500` failures; route smoke tests remain green (`m2-v1-marketplace-route.test.ts`, 14/14). New evidence artifact `2026-04-09T10-28-51-621Z-demo-business-pvt-ltd-m2-staging-validation.json`: marketplace check ? (`8 apps`) + calls linkage check ? (`1/1 linked`); only SDR audit trace remains pending.
- `2026-04-09` ? **M2 evidence gate status refresh** ? Revalidated artifact `2026-04-09T10-28-51-621Z-demo-business-pvt-ltd-m2-staging-validation.json`; gate had improved to 2/3 checks (marketplace + calls) with SDR audit trace still blocked by permission scope on `/api/v1/audit/actions` (`403 PERMISSION_DENIED` without `crm:audit:read` or `crm:admin`).
- `2026-04-09` ? **M2 evidence gate fully passed** ? Added admin permission baseline in login token payload for admin roles (`crm:admin`, `crm:audit:read`, `crm:sdr:read`, `crm:sdr:write`) and re-ran collector on localhost; new artifact `2026-04-09T10-51-38-761Z-demo-business-pvt-ltd-m2-staging-validation.json` reports `overall.pass: true` (marketplace ?, calls linkage ?, SDR audit trace ?).
- `2026-04-09` ? **Recommended next-steps closeout verification** ? Re-verified both checklists contain no open or partial items (`[ ]` / `[~]`) and confirmed latest M2 evidence remains green (`2026-04-09T10-51-38-761Z-demo-business-pvt-ltd-m2-staging-validation.json`).
- `2026-04-09` ? **P0 SMTP send + telephony signature verification + social provider expansion** ? SMTP test endpoint now sends a real email over tenant SMTP credentials (`/api/settings/smtp/test`), telephony webhook now validates Twilio signatures and surfaces verification status in settings (`/api/calls/webhook`, `/api/settings/telephony`), and social settings now support manual token connect + connection tests for Facebook/Instagram/Twitter (`/api/settings/social/connect`, `/api/settings/social/test`), with Meta refresh support for Facebook/Instagram (`/api/settings/social/refresh`).
- `2026-04-09` ? **P3 lint baseline source-of-truth freeze** ? Canonical baseline refreshed by a successful full run: `docs/evidence/lint/2026-04-09T12-00-31-469Z-dashboard-lint-baseline.json` (`47 errors, 168 warnings`, target met). Earlier re-run artifact `docs/evidence/lint/2026-04-09T11-52-47-791Z-dashboard-lint-baseline.json` timed out (no numeric summary) and is retained for traceability.
- `2026-04-09` ? **P0 social connector UX follow-up** ? Replaced non-LinkedIn prompt-based token capture with an in-page manual token form on `settings/[tenantId]/Integrations/Social` (token, account name/email, expiry seconds), preserving read-only guardrails and existing connect/test/disconnect flows.
- `2026-04-09` ? **Integration settings smoke coverage add-on** ? Added route smoke tests for `POST /api/settings/social/connect` and `GET /api/settings/telephony` signature-status behavior (`__tests__/m2/m2-settings-social-connect-route.test.ts`, `__tests__/m2/m2-settings-telephony-route.test.ts`; targeted run: 2 suites, 5 tests, all passing). Added root integration re-export shims (`lib/integrations/{permissions,audit,security}.ts`) to align test alias resolution with dashboard integration utilities.
- `2026-04-09` ? **Full smoke re-baseline after integration hardening** ? Full `npm run test:m2:smoke` re-run is green after updating webhook test mocks/expectation for telephony lookup + missing-status behavior (`46 passed suites, 1 skipped template; 264 passed, 1 skipped tests`). Full `npm run test:m3:smoke` also re-run green (`12 suites, 106 passed tests`).
- `2026-04-09` ? **Typecheck revalidation snapshot** ? Ran `npm run typecheck:dashboard`; failures remain in broader pre-existing API/UI type debt (AuditLog timestamp field usage, CPQ quote/invoice typing, admin/settings pages, etc.). Integration follow-up change in `settings/[tenantId]/Integrations/Social/page.tsx` was adjusted for strict provider typing (`facebook|instagram|twitter` cast on manual-token mutation payload).
- `2026-04-09` ? **Typecheck debt burn-down pass (API + settings)** ? Fixed `AuditLog` timestamp alignment for AI decisions + KPI scorecard (`/api/v1/ai/decisions/*`, `/api/v1/kpi/scorecard`), switched integration alerts/diagnostics email OAuth expiry checks to `OAuthIntegration` (instead of non-existent `EmailAccount.expiresAt`), and resolved settings toast/type regressions (`settings/[tenantId]/{AdminConsole,Policies,Integrations}`).
- `2026-04-09` ? **Typecheck debt burn-down pass (CPQ/quotes/inventory)** ? Updated quote/invoice conversion routes to current Prisma schema (`customerId` + JSON `items` invoice payload, audit-log idempotency, `QuoteLineItem.productName/discount`), removed legacy `convertedInvoiceId` quote assumptions, and fixed inventory reorder approve vendor scope + PO line item required fields (`tenantId`, `taxAmount`, `total`).
- `2026-04-15` ? **AI specialists + AGENTS checklist track added** ? Added "Active Track ? AI Specialists + AGENTS Alignment (2026-04)" with implemented items (specialist policy/audit/activity UI + copy standardization rollout), open gaps, and explicit maintenance rule to keep this checklist updated with every next-step implementation.
- `2026-04-15` ? **Specialist policy + audit automated tests added** ? Added policy matrix tests (`lib/ai/specialists/__tests__/policy.test.ts`) and specialist chat route audit tests for deny + granted personal-query filter paths (`apps/dashboard/app/api/ai/chat/__tests__/route.test.ts`), and marked the corresponding AI Specialists checklist gap complete.
- `2026-04-15` ? **CRM GA closeout board sync** ? Added P3 subsection for CRM Core GA runbook-driven closeout status, including closure-queue execution items and runtime blockers (Jest startup hang + authenticated speed baseline pending) aligned with `docs/LAUNCH_CHECKLIST.md`.
- `2026-04-15` ? **CRM GA closure execution log** ? Added `docs/CRM_GA_CLOSURE_EXECUTION_LOG.md` and linked it from launch queue/checklist for per-item execution evidence + blocker tracking.
- `2026-04-15` ? **CRM auth speed blocker evidence** ? Ran `node scripts/crm-auth-speed-sample.mjs`; run blocked with `Set TENANT_ID and AUTH_TOKEN...`; synced blocker details in launch checklist + closure execution log + next-actions queue.
- `2026-04-15` ? **CRM unit-test blocker repro expansion** ? Re-attempted CRM unit-test confirmation using both npm scripts and direct-file jest invocation; both paths hung in agent session. Updated blocker wording in launch checklist + closure execution log + next-actions queue.
- `2026-04-15` ? **CRM runtime blocker command pack** ? Added copy-paste rerun commands for queue #13/#14 in `docs/CRM_GA_CLOSURE_EXECUTION_LOG.md` and linked usage from launch checklist + next-actions queue.
- `2026-04-15` ? **CRM runtime evidence paste template** ? Added standardized queue #13/#14 paste-back evidence block in `docs/CRM_GA_CLOSURE_EXECUTION_LOG.md` and linked it from launch checklist + next-actions queue.
- `2026-04-15` ? **CRM runtime blocker collector automation** ? Added `scripts/run-crm-closure-blockers.mjs` and npm script `collect:crm-closure-blockers` to run queue #13/#14 checks and write evidence artifact under `docs/evidence/closure/`; linked in launch checklist + next-actions queue + closure log.
- `2026-04-15` ? **CRM blocker collector evidence run** ? Executed `npm run collect:crm-closure-blockers`; artifact `docs/evidence/closure/2026-04-15T09-55-22-392Z-crm-closure-blockers.md` captured queue #13 timeouts (all 4 tests) and queue #14 env-var blocker (`TENANT_ID`/`AUTH_TOKEN`).
- `2026-04-15` ? **CRM blocker collector mode expansion** ? Updated `scripts/run-crm-closure-blockers.mjs` to support skip modes and configurable timeout via env vars; added npm scripts `collect:crm-closure-blockers:tests-only` and `collect:crm-closure-blockers:auth-only`; documented usage in closure log + launch queue/checklist.
- `2026-04-15` ? **CRM blocker collector mode validation** ? Executed `npm run collect:crm-closure-blockers:auth-only`; artifact `docs/evidence/closure/2026-04-15T10-05-26-506Z-crm-closure-blockers.md` confirmed queue #14 remains blocked only by missing `TENANT_ID`/`AUTH_TOKEN`.
- `2026-04-15` ? **Agent Activity KPI analytics added** ? Added KPI cards for total activity, blocked-action rate, top specialist, and top module trends on `settings/[tenantId]/Agent-Activity`, computed from the current filtered activity table.
- `2026-04-15` ? **Page-scoped AI conformance audit + marketplace shell fix** ? Audited tenant module layouts for right-rail AI coverage, identified `apps/dashboard/app/marketplace/[tenantId]/layout.tsx` as pass-through, migrated it to `AppShell`, and documented scope/findings/exceptions in `docs/ai/page-ai-conformance-audit-2026-04-15.md`.
- `2026-04-15` ? **AI specialist release-readiness pass completed** ? Executed No-404 and Code Review Specialist gates for the AI specialist stack (chat/specialists APIs, assistant UI, activity page, and shell coverage), with findings and residual risk logged in `docs/ai/release-readiness-pass-2026-04-15.md`.
- `2026-04-15` ? **Specialist action approval hardening completed** ? Added explicit approval UX in `components/ai/PageAIAssistant.tsx` for `guarded_write`/`restricted`, enforced server-side approval gate in `/api/ai/chat` returning `SPECIALIST_APPROVAL_REQUIRED` when missing, and expanded route tests in `apps/dashboard/app/api/ai/chat/__tests__/route.test.ts`.
- `2026-04-15` ? **SpecialistActivityLog runbook evidence attached (in-progress closeout)** ? Added execution evidence log `docs/ai/specialist-activity-migration-evidence-2026-04-15.md` and updated the migration runbook with evidence attachment gate; closeout remains open pending stable Prisma command execution + Agent Activity UI verification in target environments.
- `2026-04-15` ? **Specialist migration verifier tooling added** ? Added `scripts/verify-specialist-activity-migration.mjs` + npm command `verify:specialist-activity-migration` to validate table/columns directly via `pg` and write evidence artifacts to `docs/evidence/ai/`; current runs show `SpecialistActivityLog` missing on the active DB target.
- `2026-04-15` ? **Dedicated SpecialistActivityLog migration added** ? Added `prisma/migrations/20260415103000_add_specialist_activity_log/migration.sql` and updated migration runbook/evidence docs; deploy apply is still pending due unstable Prisma migration execution/connectivity in this session.
- `2026-04-15` ? **Specialist migration direct-apply recovery + PASS verification** ? Added `scripts/apply-specialist-activity-migration.mjs` + npm command `db:apply:specialist-activity-migration` (pg-based SQL apply path), executed apply successfully, captured verifier PASS artifact `docs/evidence/ai/2026-04-15T10-23-41-846Z-specialist-activity-migration-verification.md`, and added targeted `.gitignore` exception so `prisma/migrations/20260415103000_add_specialist_activity_log/migration.sql` can be tracked; remaining closeout is now limited to final Agent Activity UI verification.
- `2026-04-15` ? **Runbook closeout completed via UI-status parity verification** ? Added `scripts/verify-specialist-activity-ui-status.mjs` + npm command `verify:specialist-activity-ui-status`, executed PASS with artifact `docs/evidence/ai/2026-04-15T10-28-13-971Z-specialist-activity-ui-status-verification.md` confirming expected Agent Activity status badges (`Specialist table`, `Migration ready`), and marked Runbook execution closeout complete.
- `2026-04-15` ? **CRM blocker rerun with extended timeout (still blocked)** ? Re-ran `collect:crm-closure-blockers` with `CRM_CLOSURE_TEST_TIMEOUT_MS=180000`; new artifact `docs/evidence/closure/2026-04-15T10-30-05-015Z-crm-closure-blockers.md` shows all queue #13 Jest commands still timeout at 180s and queue #14 still blocked by missing `TENANT_ID`/`AUTH_TOKEN`.
- `2026-04-15` ? **CRM auth env-readiness helper added** ? Added `scripts/check-crm-closure-env.mjs` + npm command `check:crm-closure-env`, generated artifact `docs/evidence/closure/2026-04-15T10-50-25-519Z-crm-closure-env-readiness.md` (currently missing `TENANT_ID` and `AUTH_TOKEN`), and wired the command into launch/closure blocker run instructions.
- `2026-04-15` ? **CRM direct-file Jest blocker evidence added** ? Attempted queue #13 with explicit Jest file paths (`--runTestsByPath` + `--watchAll=false`), command still hung at startup; documented evidence in `docs/evidence/closure/2026-04-15T10-52-45-775Z-crm-jest-direct-run-blocker.md` and synced launch/closure logs.
- `2026-04-15` ? **CRM queue #13 blocker cleared (scoped Jest config)** ? Added `jest.crm-closure.config.js` and updated `test:crm:*` scripts to run explicit files via scoped config; re-ran `collect:crm-closure-blockers:tests-only` and captured all queue #13 tests passing in `docs/evidence/closure/2026-04-15T11-01-56-039Z-crm-closure-blockers.md`.
- `2026-04-15` ? **CRM auth baseline one-shot automation added** ? Added `scripts/run-crm-auth-baseline.mjs` + npm command `collect:crm-auth-baseline` (login + tenant/token resolve + sampler + artifact). Run artifacts show progress from missing-env blocker to successful login/tenant resolution (`2026-04-15T11-10-04-503Z`) with remaining intermittent local request aborts on follow-up runs (`2026-04-15T11-11-53-047Z`, `2026-04-15T11-12-40-668Z`).
- `2026-04-15` ? **CRM auth baseline runner hardened + fast-probe evidence** ? Updated `run-crm-auth-baseline.mjs` to use inline endpoint sampling with retry controls and updated `crm-auth-speed-sample.mjs` for timeout/sample-count env overrides. Latest fast probes (`2026-04-15T11-21-52-930Z`, `2026-04-15T11-22-43-856Z`) indicate current blocker is local login endpoint health aborts rather than missing credentials.
- `2026-04-15` ? **CRM auth baseline stable-env probe** ? Queried latest ready Vercel deployment and ran `collect:crm-auth-baseline` against it. Endpoint is reachable, but known demo credentials (`admin@demo.com`, `businessadmin@demobusiness.com`) returned `401 Login failed` (`docs/evidence/closure/2026-04-15T11-31-19-425Z-crm-auth-baseline-run.md`, `...11-32-26-304Z...`), so queue #14 blocker is now narrowed to valid target-environment credential availability.
- `2026-04-15` ? **CRM auth baseline progressed to tasks runtime blocker** ? Registered a fresh test tenant/user on stable deployment and re-ran `collect:crm-auth-baseline`; login + tenant resolution succeeded and `contacts`/`deals` p95 were captured. Updated sampler now auto-falls back from `/api/tasks` to `/api/crm/tasks` when route is missing. Latest evidence (`docs/evidence/closure/2026-04-15T11-47-46-975Z-crm-auth-baseline-run.md`) confirms blocker is now narrowed to deployed tasks runtime failure (`/api/crm/tasks` returns `500`).
- `2026-04-15` ? **Queue #14 runtime-log evidence added** ? Re-ran baseline (`docs/evidence/closure/2026-04-15T11-52-02-618Z-crm-auth-baseline-run.md`) and captured deployment runtime logs in `docs/evidence/closure/2026-04-15T11-53-30-000Z-crm-auth-baseline-vercel-runtime-log-notes.md`; logs show `/api/auth/login` success and `/api/crm/tasks` failing with `SyntaxError: Unexpected token '<'` (HTML upstream response parsed as JSON), confirming tasks runtime/path parity as the remaining blocker.
- `2026-04-15` ? **Queue #14 tasks route fix implemented (pending deploy validation)** ? Replaced `/api/crm/tasks` proxy behavior with native task list/create handling (mirroring `/api/tasks` logic) in `apps/dashboard/app/api/crm/tasks/route.ts` to remove upstream HTML/JSON parse failure path. Next step is deploy + rerun `npm run collect:crm-auth-baseline` to confirm 3/3 endpoint sampling passes and close queue #14.
- `2026-04-15` ? **Queue #14 deploy validation attempt started** ? Triggered `vercel --prod --yes` from workspace to validate the `/api/crm/tasks` fix on live environment; command stalled at "Deploying .../payaid-v3" with no new deployment record in `vercel ls`, so rollout verification remains pending via next successful deploy run.
- `2026-04-15` ? **Queue #14 deploy blocker narrowed** ? Retried production deploy with `--force`; Vercel returned explicit limit error (`files should NOT have more than 15000 items, received 28408`) and recommended `--archive=tgz`. Archive-mode deploy attempts (root and `apps/dashboard`) were then executed but stalled at "Deploying ..." without generating a new deployment record. Queue #14 verification remains blocked on successful deployment completion.
- `2026-04-15` ? **Queue #13 flake-watch rerun evidence** ? Re-ran `collect:crm-closure-blockers:tests-only` with `CRM_CLOSURE_TEST_TIMEOUT_MS=300000`; artifact `docs/evidence/closure/2026-04-15T12-24-33-070Z-crm-closure-blockers.md` shows 3/4 pass (`merge-key`, `merge-guard`, `rbac`) with intermittent timeout on `tasks-filters`, so queue #13 remains completed with stability watch.
- `2026-04-15` ? **Queue #14 deploy handoff runbook update** ? Added deploy-capable environment command pack to `docs/CRM_GA_CLOSURE_EXECUTION_LOG.md` (archive deploy, runtime checks, baseline rerun) and linked usage from launch checklist + next-actions queue.
- `2026-04-15` ? **Queue #14 handoff command fix** ? Verified `check:dashboard:latest-runtime` / `collect:dashboard:runtime` are not present in current `package.json`; updated closure log handoff to use direct `vercel ls` + `vercel logs` checks before rerunning `collect:crm-auth-baseline`.
- `2026-04-15` ? **Queue #14 local rerun evidence refresh** ? Executed `npm run collect:crm-auth-baseline`; new artifact `docs/evidence/closure/2026-04-15T12-43-24-144Z-crm-auth-baseline-run.md` captured blocked localhost health probes (`This operation was aborted`) and no sampler payload, so deployment-capable rerun remains required to close queue #14.
- `2026-04-15` ? **Queue #14 blocker shifted to build OOM + mitigation coded** ? Latest Vercel build shows deployment extraction reduced to `7718` files, but build still fails with `Command "NODE_OPTIONS=--max-old-space-size=6144 npm run build" exited with SIGKILL` and OOM report. Added Vercel-oriented build mitigation by switching `apps/dashboard` build script to wrapper (`apps/dashboard/scripts/next-build.cjs`) that uses Turbopack on Vercel, and disabling `optimizePackageImports` on Vercel in `apps/dashboard/next.config.mjs` to lower compile memory pressure.
- `2026-04-15` ? **Queue #14 deploy diagnostics evidence captured** ? Collected `vercel inspect --logs` output for latest failed production deployments (`dpl_64a16DK8XjSyxGQRhF9EfAJTVG1N`, `dpl_82CJJ37x6B8sET28fCWmi9wt2JA1`) and recorded reproducible OOM/SIGKILL blocker details in `docs/evidence/closure/2026-04-15T13-10-00-000Z-crm-auth-baseline-vercel-build-oom-notes.md`.
- `2026-04-15` ? **Queue #14 deploy retry isolated install-stage blocker** ? Ran two targeted dashboard archive deploy retries with debug output (`dpl_EBDFPagz48QrkibJ9aUc1FpVJ49s`, `dpl_EEwfNXXJqrBAMwLmZeXMp8hx49UZ`); both fail before build compile at Vercel install step (`npm error Tracker "idealTree" already exists` for `cd ../.. && npm install --legacy-peer-deps --no-audit --no-fund`). Evidence: `docs/evidence/closure/2026-04-15T13-26-00-000Z-crm-auth-baseline-vercel-install-blocker-notes.md`.
- `2026-04-15` ? **Queue #14 Turbopack/Bull blocker captured + mitigation coded** ? Latest Vercel run on commit `d997288` reaches build and fails with Turbopack module resolution errors for Bull child-process internals (`Can't resolve ... bull/lib/process/master.js`). Added evidence note `docs/evidence/closure/2026-04-15T19-04-38-996Z-vercel-turbopack-bull-build-blocker-notes.md` and coded mitigation `serverExternalPackages: ['bull', 'ioredis']` in `apps/dashboard/next.config.mjs` to externalize Bull during server bundling.
- `2026-04-15` ? **Queue #14 redeploy retriggered after Bull externalization** ? Triggered fresh `vercel --prod --archive=tgz` from workspace after applying `serverExternalPackages` mitigation; local CLI still stalls at deploy start in this environment, so verification now depends on hosted Vercel run outcome for the latest commit.
- `2026-04-15` ? **Queue #14 build fallback mitigation added** ? Updated `apps/dashboard/scripts/next-build.cjs` to auto-retry `next build --webpack` when Vercel Turbopack build fails (compatibility fallback for Bull server-relative import resolution), while keeping Turbopack as first attempt for lower memory pressure.
- `2026-04-15` ? **Queue #14 install-command iteration evidence captured** ? Tested `apps/dashboard/vercel.json` install command variants: `npm ci` resolves `idealTree` path but fails on Vercel with lockfile `EUSAGE` (`dpl_8eAdGuXFeQ8hot5nUfVzoMu7N5xC`), then follow-up `npm install --no-audit --no-fund` retry stalls at local tarball packing before deployment creation in this workstation session. Evidence: `docs/evidence/closure/2026-04-15T13-46-00-000Z-crm-auth-baseline-vercel-install-command-iteration-notes.md`.
- `2026-04-15` ? **Queue #14 install-variant matrix expanded** ? Ran non-archive hosted deploy retries with additional install variants: plain install (`dpl_5hckab3X87fADw8cvrQoWZZmnA6N`) and prefix install (`dpl_9dTMnXRhYiCrii7pFtjdGcs7LBZu`) both still fail with `npm error Tracker "idealTree" already exists`; workspace-scoped variant (`dpl_4Y4PrjTkyHMLStwnU1aKwJzvenQt`) fails with `/package.json` ENOENT. Restored `apps/dashboard/vercel.json` install command to legacy baseline and logged full matrix in `docs/evidence/closure/2026-04-15T14-14-00-000Z-crm-auth-baseline-vercel-install-variant-matrix-notes.md`.
- `2026-04-15` ? **AGENTS operationalization template added** ? Added `docs/ai/specialist-execution-template.md` and linked it from `AGENTS.md` Quick Start so teams can run Product Strategist -> Platform Architect -> Domain -> No-404 QA -> Code Review with evidence and ship gates.
- `2026-04-15` ? **AGENTS first live-ticket run completed** ? Added `docs/ai/specialist-first-run-2026-04-15-queue14.md` with a fully filled specialist execution for active Queue #14 (CRM auth speed baseline/deploy blocker), including specialist outputs, evidence capture, and non-ship gate decision.
- `2026-04-15` ? **Queue #14 post-successful-build baseline rerun confirms runtime blocker** ? After hosted deploy logs showed compile + static generation success, reran `collect:crm-auth-baseline` against `https://payaid-v3.vercel.app`; login and contacts/deals p95 succeed, but `/api/crm/tasks` still returns `500` (artifact `docs/evidence/closure/2026-04-15T14-41-33-119Z-crm-auth-baseline-run.md`), so queue #14 remains open on tasks runtime parity.
- `2026-04-15` ? **Queue #14 runtime logs refreshed on successful deployment** ? Captured live `vercel logs` during baseline replay; `/api/crm/tasks` still throws `SyntaxError: Unexpected token '<'` with HTML payload parsing, confirming deployed runtime behavior mismatch persists despite successful build/deploy. Evidence: `docs/evidence/closure/2026-04-15T14-45-00-000Z-crm-auth-baseline-runtime-log-refresh.md` and `docs/evidence/closure/2026-04-15T14-45-06-557Z-crm-auth-baseline-run.md`.
- `2026-04-15` ? **Platform pre-demo audit run published** ? Executed `lint`, `typecheck`, `m2 smoke`, `m3 smoke`, and partial `route-health` checks; published severity-ranked failures and fix order in `docs/PLATFORM_PREDEMO_AUDIT_2026-04-15.md` (blocking routes: `/home/demo`, `/crm/demo/Home`, `/crm/demo/Activities`, `/crm/demo/Leads`; blocking suites: M2 CPQ/signals/audit/KPI scorecard).

- `2026-04-16` - **Lint exhaustive-deps batch L3 (CRM Home pair) + full re-baseline** - Stabilized hook dependencies in `crm/[tenantId]/Home/page.tsx` and `crm/[tenantId]/Home/CRMHomeDashboard.tsx` using `useCallback` + stable refs for pre-declaration effect call-sites (`fetchDashboardStats` / `fetchActivityFeed`) and fixed missing perf/demo effect deps. Full artifact `apps/dashboard/docs/evidence/lint/2026-04-16-dashboard-lint-full-post-batch-l3.json` shows **0** errors, **49** warnings, **39** files (`exhaustive-deps` `60 -> 49`, `set-state-in-effect` stays **0**). `typecheck:dashboard` was attempted repeatedly but did not emit an exit footer in this environment; local/CI typecheck confirmation is pending for this batch.
- `2026-04-16` - **Speed verification tooling added (runbook + benchmark script)** - Added reusable latency benchmark command `npm run bench:api-timing` via `scripts/bench-api-timing.mjs` (notifications + AI chat, client and server-timing p50/p95 summaries) and documented execution/evidence workflow in `docs/API_TIMING_VERIFICATION_RUNBOOK.md`; local smoke run confirms script wiring and explicit token precondition messaging.
- `2026-04-16` - **Speed verification artifact mode added** - Extended `scripts/bench-api-timing.mjs` with opt-in evidence export (`BENCH_WRITE_ARTIFACT=1`) to timestamped JSON under `docs/evidence/performance/` (configurable via `BENCH_ARTIFACT_DIR`), added `npm run bench:api-timing:artifact`, and updated `docs/API_TIMING_VERIFICATION_RUNBOOK.md` with artifact workflow/filenames; smoke run confirms command wiring and token precondition messaging.
- `2026-04-16` - **Speed verification compare mode added** - Extended `scripts/bench-api-timing.mjs` with artifact diff mode (`BENCH_COMPARE=1`) and added `npm run bench:api-timing:compare` to report baseline vs candidate deltas for notifications/AI chat client+server p50/p95; updated `docs/API_TIMING_VERIFICATION_RUNBOOK.md` with compare command usage, and smoke run confirms required artifact-path guardrails.
- `2026-04-16` - **Speed verification regression gate added** - Extended compare mode with optional threshold enforcement (`BENCH_FAIL_ON_REGRESSION=1`) and configurable p95 budgets (`BENCH_MAX_CLIENT_P95_REGRESSION_PCT`, `BENCH_MAX_SERVER_P95_REGRESSION_PCT`), added `npm run bench:api-timing:gate`, and documented gate usage in `docs/API_TIMING_VERIFICATION_RUNBOOK.md`; smoke run confirms gate command wiring and required artifact-path guardrails.
- `2026-04-16` - **GitHub Actions timing gate workflow added** - Added `.github/workflows/api-timing-gate.yml` to run p95 regression checks in CI (PR path-triggered artifact comparison + manual dispatch with explicit artifact paths/thresholds), and updated `docs/API_TIMING_VERIFICATION_RUNBOOK.md` with CI gate behavior and usage.
- `2026-04-16` - **Timing gate PR comment automation added** - Enhanced `.github/workflows/api-timing-gate.yml` to install deps, capture gate output, post/update a PR comment with p95 summary + pass/fail status via `actions/github-script`, and fail the workflow after comment publication when regression thresholds are breached.
- `2026-04-16` - **Timing gate artifact selection hardened** - Updated `.github/workflows/api-timing-gate.yml` to prefer explicit artifact filename conventions (`baseline|before` and `candidate|after`) with lexicographic fallback, reject identical baseline/candidate resolution, and include selection reason in PR comment output; runbook updated to document the new selection behavior.
- `2026-04-16` - **Timing artifact naming guidance + optional strict enforcement** - Added an explicit artifact naming convention section in `docs/API_TIMING_VERIFICATION_RUNBOOK.md` and enhanced `.github/workflows/api-timing-gate.yml` with warning-first behavior plus optional hard-fail enforcement (`enforce_artifact_naming` in workflow dispatch) when baseline/candidate naming conventions are not followed.
- `2026-04-16` - **Timing artifact naming helper + role-based capture shortcuts** - Added `scripts/generate-api-timing-artifact-name.mjs` for convention-safe baseline/candidate filenames, extended `scripts/bench-api-timing.mjs` with `BENCH_ARTIFACT_ROLE` naming support, and added npm shortcuts (`bench:api-timing:artifact:{baseline,candidate,name}`) with runbook updates to reduce artifact naming mistakes.
- `2026-04-16` - **Timing artifact pair-capture command added** - Added `scripts/capture-api-timing-pair.mjs` and npm shortcut `bench:api-timing:artifact:pair` to run baseline + candidate capture sequentially in one flow (optional `BENCH_PAIR_WAIT_SECONDS` delay), with runbook updates and compare-command handoff output.
- `2026-04-16` - **Timing end-to-end pipeline command added** - Added `scripts/run-api-timing-pipeline.mjs` and npm shortcut `bench:api-timing:pipeline` to execute baseline capture, candidate capture, compare summary, and regression gate sequentially in one command, with runbook updates for one-shot local/CI validation flow.
- `2026-04-16` - **Timing pipeline canonical summary artifact added** - Enhanced `scripts/run-api-timing-pipeline.mjs` to always emit a single `*-api-timing-pipeline-summary.json` artifact (status, step outcomes, artifact paths, thresholds, and error context) under `docs/evidence/performance/` by default, with `BENCH_PIPELINE_SUMMARY_DIR` and `BENCH_PIPELINE_WRITE_SUMMARY=0` controls documented in runbook.
- `2026-04-16` - **Timing summary verdict command added** - Added `scripts/print-api-timing-verdict.mjs` and npm shortcut `bench:api-timing:verdict` to read a pipeline summary artifact (explicit path or latest in summary dir), print a CI-friendly `RELEASE_READY`/`BLOCKED` verdict line with step state, and exit non-zero on blocked status.
- `2026-04-16` - **Timing workflow verdict parser integration added** - Enhanced `.github/workflows/api-timing-gate.yml` to build a gate summary JSON, run `bench:api-timing:verdict`, include the standardized `API_TIMING_VERDICT` line in PR comments, and fail the workflow if either gate or verdict parser exits non-zero.
- `2026-04-16` - **Timing gate diagnostics artifact upload added** - Enhanced `.github/workflows/api-timing-gate.yml` with an always-run upload step (`actions/upload-artifact`) to persist `gate-output.txt`, `verdict-output.txt`, and `api-timing-gate-summary.json` as `api-timing-gate-diagnostics-*` for CI audit/debug traceability; documented in runbook.
- `2026-04-16` - **Lint exhaustive-deps batch L4 (Deals + Bank-Reconciliation) + full re-baseline** - Stabilized memo dependencies in `crm/[tenantId]/Deals/page.tsx` (memoized `pipelineSummary` + `deals`, direct period-bound call in stats memo) and `finance/[tenantId]/Bank-Reconciliation/page.tsx` (memoized `rawTransactions` and `apiBankAccounts`). Full artifact `apps/dashboard/docs/evidence/lint/2026-04-16-dashboard-lint-full-post-batch-l4.json` shows **0** errors, **43** warnings, **40** files (`exhaustive-deps` `49 -> 43`, `set-state-in-effect` stays **0**). `typecheck:dashboard` completed with one pre-existing unrelated error in `apps/dashboard/app/api/notifications/route.ts`; L3-introduced CRM Home `currentView` declaration-order regression was fixed.
- `2026-04-16` - **Lint exhaustive-deps batch L5 (Cofounder + Credit/Debit Notes) + full re-baseline** - Stabilized hook dependencies in `ai-studio/[tenantId]/Cofounder/page.tsx`, `finance/[tenantId]/Credit-Notes/new/page.tsx`, and `finance/[tenantId]/Debit-Notes/new/page.tsx` using memoized fallback arrays (`agents`/`contacts`) and extracted item-recalc dependency signatures. Full artifact `apps/dashboard/docs/evidence/lint/2026-04-16-dashboard-lint-full-post-batch-l5.json` shows **0** errors, **37** warnings, **37** files (`exhaustive-deps` `43 -> 37`, `set-state-in-effect` stays **0**). `typecheck:dashboard` again exits with the same pre-existing unrelated error in `apps/dashboard/app/api/notifications/route.ts`.
- `2026-04-17` - **Lint exhaustive-deps batch L6 (Knowledge + AllPeople + Tasks + Visitors) + full re-baseline** - Stabilized hook dependencies by memoizing fallback arrays and callback functions in `ai-studio/[tenantId]/Knowledge/page.tsx`, `crm/[tenantId]/AllPeople/page.tsx`, `crm/[tenantId]/Tasks/page.tsx`, and `crm/[tenantId]/Visitors/page.tsx`. Full artifact `apps/dashboard/docs/evidence/lint/2026-04-16-dashboard-lint-full-post-batch-l6.json` shows **0** errors, **33** warnings, **33** files (`exhaustive-deps` `37 -> 33`, `set-state-in-effect` stays **0**). `typecheck:dashboard` again exits with the same pre-existing unrelated error in `apps/dashboard/app/api/notifications/route.ts`.
- `2026-04-17` - **PayAid V3 product guide authored** - Added comprehensive internal guide `docs/payaid-v3-product-guide.md` covering platform overview, module catalog (4.1-4.30), AI specialist model, core workflows, readiness/status framing, onboarding, FAQ/objections, sales guidance, architecture, roadmap gaps, competitive positioning, and tester implementation notes.
- `2026-04-17` - **CRM closeout blocker remediation + checkpoints refresh** - Cleared queue #13 false-timeout by raising closure test timeout default (`scripts/run-crm-closure-blockers.mjs`), hardened task list APIs (`apps/dashboard/app/api/tasks/route.ts`, `apps/dashboard/app/api/crm/tasks/route.ts`) with fallback payload path, and captured fresh closure evidence (`docs/evidence/closure/2026-04-17T02-24-28-340Z-crm-closure-blockers.md`, `docs/evidence/closure/2026-04-17T02-41-31-970Z-crm-auth-baseline-run.md`). Synced queue status + speed row updates in launch docs and published module feature checkpoint file `docs/CRM_MODULE_FEATURE_GO_NO_GO_2026-04-17.md`.
- `2026-04-17` - **No-404 + CI gate hardening pass** - Fixed dashboard typecheck regression in `apps/dashboard/app/api/notifications/route.ts` (narrow `missingAttendance` type to selected fields). Added `forceExit` to `jest.m2.smoke.config.js` / `jest.m3.smoke.config.js` so smoke suites exit cleanly. Hardened `tests/route-health.spec.ts` with transient HTTP 500 retries for `/crm/*` and `/home/*`, and pinned `test:e2e:route-health*` scripts to `--project chromium` to avoid duplicate route hits across Playwright projects hammering one Next dev server. Extended `scripts/run-crm-auth-baseline.mjs` with optional warmup (`CRM_AUTH_WARMUP_ROUNDS`, default `2`); refreshed hosted baseline artifact `docs/evidence/closure/2026-04-17T04-42-19-970Z-crm-auth-baseline-run.md` and updated `docs/CRM_MODULE_FEATURE_GO_NO_GO_2026-04-17.md` gate table (speed SLO vs hosted alias remains open).
- `2026-04-17` - **CRM task list cache pass + route-health full-run started** - Added multi-layer caching and invalidation for CRM task list GET endpoints (`/api/tasks`, `/api/crm/tasks`) with shared key builder + TTL helper (`lib/crm/tasks-list-cache.ts`), plus invalidation on task create/update/delete (`apps/dashboard/app/api/tasks/route.ts`, `apps/dashboard/app/api/crm/tasks/route.ts`, `apps/dashboard/app/api/tasks/[id]/route.ts`). Started full `npm run test:e2e:route-health:serial`; run is currently progressing with repeated CRM route failures/timeouts after initial routes, so release gate remains open pending final failure summary + fixes.
- `2026-04-17` - **CRM route-health rerun + hosted speed re-baseline** - Ran focused route-health rerun for prior CRM failures (`Leads`, `Contacts`, `AllPeople`, `Deals`) and got 4/4 pass, then restarted the full serial matrix (`npm run test:e2e:route-health:serial`) now progressing with green results through the first 33 routes at capture time. Re-ran hosted auth baseline with warmup (`CRM_AUTH_WARMUP_ROUNDS=3 CRM_AUTH_SAMPLE_COUNT=15 npm run collect:crm-auth-baseline`), producing `docs/evidence/closure/2026-04-17T06-10-14-447Z-crm-auth-baseline-run.md` (p95 ms: contacts 3169, deals 3411, tasks 3376), so speed SLO remains open.
- `2026-04-17` - **Route-health timeout/retry hardening for projects/inventory + targeted rerun passes** - Extended route-health stability tuning in `tests/route-health.spec.ts`: marked `/projects/*` and `/inventory/*` as extra-slow (8 min request timeout) and broadened transient HTTP 500 retry logic to all non-auth app routes. This cleared deterministic timeouts on `projects/demo/(Gantt|Time)` (2/2 pass) and produced a clean tail rerun (`13 passed`) for `inventory/*`, `ai-studio/*`, `productivity/Home`, `spreadsheet/*`, and `industry-intelligence/Home`. Full 79-route serial proof run remains the final open verification step.
- `2026-04-17` - **Route-health stability tuning pass 2 (CRM/Sales + default timeout)** - Further hardened `tests/route-health.spec.ts` to reduce dev-cold-compile false negatives: increased global default route timeout to 8 min, assigned `/crm/*` and `/sales/*` to an extreme-slow 15 min timeout bucket, and kept transient 500 retries for all non-auth routes. Quick validation checks now pass for `/`, `/login`, `/home/demo` and isolated `crm/demo/Deals`; full `test:e2e:route-health:serial` rerun is currently in progress after a full Node process reset.
- `2026-04-17` - **CRM UX/runtime patch pass (contacts + activities + tasks + invoice handoff)** - Replaced Contact detail placeholder cards with live CRM components (owner reassignment, nurture, intelligence, automation timeline, AI assist), switched Contact call/email/WhatsApp quick actions to internal CRM tools (`Dialer`, `inbox`, `OutboxOps`), fixed Invoice-from-contact prefill to hydrate the selected contact directly (without blocking on full contacts list), added explicit sort controls on `crm/[tenantId]/Tasks`, improved Activities type-tag contrast/readability, raised Page AI z-index above right-side detail drawer, and added AI context hydration/cleanup from activity detail actions.
- `2026-04-17` - **Header dropdown layering + console hygiene patch** - Raised profile menu above sticky header via body portal in `components/modules/ModuleTopBar.tsx`, optimized Spline hero startup in `components/landing/LottieHero.tsx` (credential-aligned preloads + render-size guard) to keep 3D while reducing preload/WebGL console churn, and updated CRM Home perf telemetry in `crm/[tenantId]/Home/page.tsx` to send auth headers only when token is available (eliminates `/api/v1/analytics/event` 401 spam).
- `2026-04-17` - **Contacts/Tasks debounced-search + visibility hardening** - Added shared search normalization helpers (`lib/search/query-normalization.ts`) and reusable debounced hook (`lib/hooks/use-debounced-search.ts`), wired Contacts (`crm/[tenantId]/AllPeople/page.tsx`) and Tasks (`crm/[tenantId]/Tasks/page.tsx`) to normalized 300ms debounced search with min-length gating, inline search status, and page-stable result refresh. Hardened API hooks (`lib/hooks/use-api.ts`) to pass AbortSignal + keep previous data/stale cache for smooth typing, and expanded task search filter semantics (`lib/crm/tasks-list-where.ts`) to match contact/assignee/id fields so contact-linked tasks assigned to other users remain discoverable.
- `2026-04-17` - **Task edit owner/save UX + pagination-stable task sorting** - Enabled assignee changes on Task detail (`crm/[tenantId]/Tasks/[id]/page.tsx`) using HR employee options, added explicit success feedback after save, and made list sorting page-size stable by sorting across full filtered task set before pagination in `crm/[tenantId]/Tasks/page.tsx` (fixes mismatch where 25/page and 100/page showed different top due dates). Raised task list API max fetch cap to 1000 in `/api/tasks` and `/api/crm/tasks` to support consistent client-side sorting for active CRM datasets.
- `2026-04-18` ? **Priority checklist aligned with GA closure board for queues #13/#14** ? Split P3 CRM GA rows: renamed the open item to **Execute CRM GA closure queue end-to-end** (Day 2?SOLO-T11) and marked **Clear CRM GA runtime blockers (queues #13 / #14)** `[x]` with evidence pointers matching `docs/LAUNCH_CHECKLIST.md` + `docs/CRM_GA_CLOSURE_EXECUTION_LOG.md` (primary queue #14 pass: `docs/evidence/closure/2026-04-17T02-41-31-970Z-crm-auth-baseline-run.md`).
- `2026-04-18` ? **Local dev CRM pipeline (auth baseline + route-health serial, single dev server)** ? Brought up `npm run dev -w dashboard`, probed readiness with `GET http://127.0.0.1:3000/api/auth/login`, then ran `collect:crm-auth-baseline` against `BASE_URL=http://127.0.0.1:3000` with `CRM_LOGIN_EMAIL` / `CRM_LOGIN_PASSWORD` per `docs/CRM_GA_CLOSURE_EXECUTION_LOG.md` (`admin@demo.com` + documented test password). Pass artifact: `docs/evidence/closure/2026-04-18T10-08-44-035Z-crm-auth-baseline-run.md` (steady-state local p95 ms after warmup: contacts 636, deals 725, tasks 1421 ? still above the 300ms GA guardrail; hosted target remains authoritative for GA SLO). Ran `PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000` + `PLAYWRIGHT_NO_WEB_SERVER=1` + `npm run test:e2e:route-health:serial` (no second dev server): **`79 passed`**, exit `0`, ~55m ? **No-404 route-health gate cleared**; synced `docs/CRM_MODULE_FEATURE_GO_NO_GO_2026-04-17.md` to **GO** for that row.
- `2026-04-18` ? **Engineering deploy readiness + `/api/deals` list fast path** ? Published `docs/DEPLOY_READINESS_2026-04-18.md` (deploy **GO** with explicit **GA speed SLO** + **Day 2?SOLO-T11 manual queue** exceptions). Updated module gate table: **Marketing/Finance/HR** ? **GO (No-404 matrix)**, **speed** ? strict **NO-GO** with **conditional deploy** note. Landed `stats=false` on `GET /api/deals` (`apps/dashboard/app/api/deals/route.ts`) to skip tenant-wide `groupBy` when callers only need list + pagination (UI default unchanged). **Push:** commit these paths and merge to main / trigger your Vercel (or equivalent) deploy.
- `2026-04-18` ? **Day 2 QA partial + hosted auth re-baseline** ? Added `tests/e2e/crm-day2-qa-postgating.spec.ts` and `npm run test:e2e:crm-day2-qa` (serial workers; requires local dev on `:3000`). Updated `docs/CRM_GA_DAY2_QA_HOME_DEALS_CONTACTS.md`, `docs/LAUNCH_CHECKLIST.md` Day 2 QA row, closure log queue #1 ? **Partial**. Ran hosted `collect:crm-auth-baseline` ? `docs/evidence/closure/2026-04-18T14-16-16-086Z-crm-auth-baseline-run.md` (p95 ms: contacts 3218, deals 2523, tasks 3787; **deals** improved vs prior hosted row; strict 300ms SLO still **Fail**). Synced speed row in `docs/LAUNCH_CHECKLIST.md` + `docs/CRM_MODULE_FEATURE_GO_NO_GO_2026-04-17.md`.
- `2026-04-18` ? **CRM Lead Routing v1 post-deploy** ? Applied `prisma migrate deploy` for migration `20260419120000_crm_config_lead_routing` (Supabase Postgres via pooler); ran `npx prisma generate` (Prisma Client v5.22.0). Code on `main`: commit `8de67c84`. **Manual smoke:** enable routing in CRM ? Automation ? Lead-Routing, submit an unassigned inbound lead, confirm `assignedToId` + orchestration log routing reason; repeat `migrate deploy` on any other DBs (staging/prod) that are not this connection.
- `2026-04-18` ? **CRM list perf (deals)** ? Added composite index `idx_deal_tenant_created_desc` on `Deal` (`tenantId`, `createdAt` DESC) via migration `20260420120000_idx_deal_tenant_created_desc`. Routed `GET /api/deals` read queries (incl. diagnostics/retry paths) through `prismaRead`; writes remain on `prisma`. **Next:** `prisma migrate deploy` on target DBs, then re-run `collect:crm-auth-baseline` on hosted to refresh p95 evidence.
- `2026-04-18` ? **CRM list perf (tasks)** ? Added composite index `idx_task_tenant_priority_due` on `Task` (`tenantId`, `priority`, `dueDate`) via migration `20260420130000_idx_task_tenant_priority_due`. Routed task list reads in `GET /api/tasks` and `GET /api/crm/tasks` (incl. fallback hydration) through `prismaRead`; mutations stay on `prisma`. **Next:** `prisma migrate deploy` + hosted `collect:crm-auth-baseline`.
- `2026-04-18` ? **Lead routing GET reliability (hosted)** ? `GET /api/crm/lead-routing` no longer uses `orderBy: { user: { name } }` on `salesRep` (avoids fragile relation sorts); reps sorted in memory; rep-query failures return empty `salesReps` so the Automation page still loads. Commit `0d5c55a9`.
- `2026-04-18` ? **Day 3 Tasks QA smoke (Playwright) + GA doc sync** ? Added `tests/e2e/crm-day3-tasks-qa-smoke.spec.ts` and `npm run test:e2e:crm-day3-qa`; updated `docs/CRM_GA_SOLO_T03_QA_RUNBOOK.md` (automation blurb), `docs/CRM_GA_NEXT_ACTIONS_QUEUE.md` (P0 #3 partial + quick commands), `docs/CRM_GA_CLOSURE_EXECUTION_LOG.md` (daily note), `docs/CRM_MODULE_FEATURE_GO_NO_GO_2026-04-17.md` (Day 3 partial automation note). **Note:** `npm run test:e2e:route-health:serial` aborted here when `verify:db` could not reach the configured Supabase pooler (`Can't reach database server`); use reachable `DATABASE_URL`, `SKIP_DB_VERIFY=1` only if intentionally bypassing the gate, or local Docker per script hint, then rerun with `npm run dev -w dashboard` + `PLAYWRIGHT_NO_WEB_SERVER=1`.
- `2026-04-19` ? **Track B hosted re-baseline + contacts list cache headers** ? Attempted `collect:crm-auth-baseline` against `https://payaid-v3.vercel.app` without `CRM_LOGIN_*` ? blocked (`Invalid email or password`); documented copy/paste env flow + `prisma migrate deploy` reminder in `docs/DEPLOY_READINESS_2026-04-18.md` (**Post-deploy** section). Added `Cache-Control: private, no-store` on `GET /api/contacts` JSON (cached + fresh) to avoid stale browser caches of authenticated list payloads. **Next:** run baseline with real `CRM_LOGIN_EMAIL` / `CRM_LOGIN_PASSWORD`, then append speed row in `docs/LAUNCH_CHECKLIST.md`.
- `2026-04-19` ? **CRM inbound pilot (engineering closeout)** ? Shipped migration `20260420150000_inbound_pilot_loop`, orchestration + ingress idempotency, verify/smoke scripts (`verify:inbound-pilot-smoke`, `smoke:inbound-pilot`, `smoke:inbound-pilot:local` with `DATABASE_DIRECT_URL=` override), local DB workflow (`db:local:baseline-migrations`, `db:local:fresh-from-schema`, `db:push:local` `--skip-generate`), Prisma `cRMConfig` delegate usage, `TenantMember` upserts in `prisma/seed.ts` + `prisma/seeds/seed-admin-users.ts`, and synced **GO (engineering)** row in `docs/CRM_MODULE_FEATURE_GO_NO_GO_2026-04-17.md`. **Still manual for GA product:** Day 2?10 queue + hosted p95 SLO per same doc.
- `2026-04-19` ? **Hosted speed re-baseline rerun (still blocked by credentials)** ? Re-ran `collect:crm-auth-baseline` with `BASE_URL=https://payaid-v3.vercel.app`; health probe passed (`200`) but login failed (`401 Invalid email or password`) for default collector credentials. Added a blocked row to `docs/LAUNCH_CHECKLIST.md` speed evidence table with artifact `docs/evidence/closure/2026-04-19T14-50-50-645Z-crm-auth-baseline-run.md`; next run must supply valid `CRM_LOGIN_EMAIL`/`CRM_LOGIN_PASSWORD`.
- `2026-04-19` ? **Hosted speed re-baseline rerun (credentialed pass, SLO still fail)** ? Re-ran `collect:crm-auth-baseline` against `https://payaid-v3.vercel.app` with valid credentials (`admin@demo.com`, warmup `3`, sample `25`); pass artifact `docs/evidence/closure/2026-04-19T14-52-28-898Z-crm-auth-baseline-run.md` captured hosted p95: contacts `2233`, deals `2189`, tasks `1894` (all `200`). Updated `docs/LAUNCH_CHECKLIST.md` speed evidence row and refreshed the speed guardrail evidence in `docs/CRM_MODULE_FEATURE_GO_NO_GO_2026-04-17.md`; strict 300ms GA SLO remains open.
- `2026-04-19` ? **Day 2 Playwright local retry (blocked)** ? Ran `crm-day2-qa-postgating.spec.ts` with `PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000` + `PLAYWRIGHT_NO_WEB_SERVER=1`; `/api/auth/login` hit 240s timeout and UI fallback failed. Correlated dev log showed `RangeError: Array buffer allocation failed` after very long cold compiles. Evidence: `docs/evidence/closure/2026-04-19T17-19-22-758Z-crm-day2-playwright-local-retry.md`; closure log queue #1 stays **Partial**. **Next:** restart dev with memory headroom, warm auth routes, rerun the spec.
- `2026-04-19` ? **SLO / GA messaging governance (docs)** ? Added **?C ? Formal SLO / GA messaging change** to `docs/CRM_MODULE_FEATURE_GO_NO_GO_2026-04-17.md` (Product + Launch DRI + SOLO-T11 + GTM). Cross-linked from `docs/DEPLOY_READINESS_2026-04-18.md`, `docs/CRM_SPEED_BASELINE_RUNBOOK.md`, and `docs/CRM_GA_SOLO_T11_DECISION_RECORD_RUNBOOK.md`; P1 note in `docs/CRM_GA_NEXT_ACTIONS_QUEUE.md`. Does not change measured p95; clears path if strict 300ms is formally revised.
- `2026-04-19` ? **CRM list API tenant resolution (perf + consistency)** ? `lib/crm/resolve-crm-request-tenant.ts`: early return when `?tenantId=` matches JWT (skips user/tenant reads on the common path); `prismaRead` for lookups; demo cross-tenant uses `NEXT_PUBLIC_CRM_ALLOW_DEMO_SEED === '1'` (aligned with prior `GET /api/contacts` / `GET /api/deals` rules). Those two list routes plus `contacts/[id]` now use the shared helper (removed duplicate local resolver on `[id]`). **Next:** deploy + hosted `collect:crm-auth-baseline` for p95 delta. **Note:** any route that previously relied on the old shared helper?s demo override *without* that env flag now matches contacts/deals ? set the flag on demo/staging if needed.
- `2026-04-19` ? **Dashboard typecheck green (blocking errors cleared)** ? Fixed `lib/crm/sales-automation-workspace-server.ts` (Prisma `as const` where inputs, `Contact` orderBy `createdAt` vs missing `updatedAt`, signal severity typing, duplicate/broken prospect queue map). Fixed `apps/dashboard/app/api/deals/route.ts` `runDealsQuery` tuple return (`pipelineSummary` readonly empty tuple). Fixed `contacts/[id]` task `notIn` readonly + `buildContact360` payload typing. Fixed dialer `Contact` orderBy. Extended `AnalyticsEventName` + `_request` in v1 quote send/approval routes; inbound dead-letter JSON double cast. **`npm run typecheck:dashboard` ? exit `0`.**
- `2026-04-20` - **Inbound pilot release gate (engineering blockers)** - **E3:** `GET /api/crm/inbound-routing/decisions` validates `contactId` (cuid-like) and safe `limit` (reject NaN). **F4:** `components/modules/ModuleSwitcher.tsx` removed `|| true` license bypass; `components/ModuleSwitcher.tsx` filters primary/secondary by `tenant.licensedModules` when `tenantId` set. **B4/B6:** `skipPilotLoopArtifacts` only skips pilot artifacts (not `skipExecutionLogWrite`); bulk `leads/import` + `marketing/contacts/upload` set `skipPilotLoopArtifacts: true`; `pilot-post-persist.ts` isolates task-create failure and retries `InboundRoutingDecision` create once. Evidence folder stub: `docs/evidence/pilot/README.md`. **Next:** QA smoke + dated artifacts per pilot gate plan; `B3` replay row-count proof still open.
- `2026-04-20` - **Inbound pilot B3 smoke + E4 template** - `scripts/smoke-inbound-pilot-integration.ts` now asserts replay leaves **InboundOrchestrationLog**, **Task** (smoke-titled), and **InboundIngressIdempotency** counts unchanged (in addition to `InboundRoutingDecision`). Added `docs/evidence/pilot/INBOUND_UI_QA_CHECKLIST.template.md` and README pointers. **Next:** run smoke against pilot DB; copy checklist to dated evidence file; fill E4.
- `2026-04-20` - **Inbound pilot smoke DX + runbook** - `--tenant` / `verify` `--tenant` resolve **`Tenant.id`, `Tenant.slug`, or `Tenant.subdomain`** (case-insensitive slug); clear error if missing. Added `docs/evidence/pilot/RUNBOOK.md` (commands for verify, smoke, curl). **Next:** seed or pick a real tenant on target DB, then save dated stdout under `docs/evidence/pilot/`; complete E4 dated checklist.
- `2026-04-20` - **Inbound pilot local evidence (subdomain + E4 stub)** - Tenant resolution extended with **`Tenant.subdomain`** so `--tenant=demo` matches `prisma/seed.ts`. **`npm run smoke:inbound-pilot:local -- --tenant=demo` -> PASSED**; artifacts `docs/evidence/pilot/2026-04-20-inbound-smoke-local.md`, `2026-04-20-inbound-db-verify-local.md`, partial `2026-04-20-inbound-ui-qa.md`. **Next:** signed-in UI + curl matrix on target URL; optional hosted smoke artifact.
- `2026-04-20` - **Inbound decisions API - M2 route smoke** - Added `__tests__/m2/m2-crm-inbound-routing-decisions-route.test.ts` (invalid `contactId` -> `400`, valid cuid -> `200` + `decisions[]`, non-numeric `limit` -> take `40`, omit `limit` -> `40`, `limit=500` -> clamp `100`). E4 rows 3-5 marked PASS (automation) in `docs/evidence/pilot/2026-04-20-inbound-ui-qa.md`; RUNBOOK section 3 documents jest command. **Next:** manual rows 1,6,7 (CRM UI + module switcher).
- `2026-04-20` - **E4 close (automation): execution logs + lead-routing + module license** - `lib/tenant/module-license-filter.ts` + both `ModuleSwitcher` surfaces use `isModuleListedForTenantLicense`; `m2-module-license-filter.test.ts`. `GET /api/crm/inbound-orchestration/logs` safe `limit` parse + `m2-crm-inbound-orchestration-logs-route.test.ts`. `GET /api/crm/lead-routing` pilot payload + `m2-crm-lead-routing-get-route.test.ts`. `2026-04-20-inbound-ui-qa.md` + RUNBOOK jest pattern updated (11 tests in combined pattern). **Next:** optional stakeholder screenshots / hosted curl.
- `2026-04-20` - **Navigation header module switcher - license parity** - `components/Navigation/ModuleSwitcher.tsx` (imported from `components/layout/header.tsx`) now filters the sheet module list with `isModuleListedForTenantLicense`, matching CRM/dashboard switchers (F4 / E4 row 7).
- `2026-04-20` - **Inbound E4 automation rerun + typecheck reconfirmed** - Re-ran combined pilot E4 M2 pattern (`m2-crm-inbound-routing-decisions-route|m2-crm-inbound-orchestration-logs-route|m2-crm-lead-routing-get-route|m2-module-license-filter`) with **4 suites / 11 tests PASS** and captured artifact `docs/evidence/pilot/2026-04-20-inbound-e4-automation-suite.md`. Re-ran `npm run typecheck:dashboard` after navigation switcher parity change - exit `0`.
- `2026-04-20` - **Hosted inbound smoke attempt logged (blocker artifact)** - Ran `npm run smoke:inbound-pilot -- --tenant=demo` against hosted pooler env; failed with `InboundOrchestrationLog_tenantId_fkey` / `InboundOrchestrationDeadLetter_tenantId_fkey` and `CONTACT_LIMIT`. Added artifact `docs/evidence/pilot/2026-04-20-inbound-smoke-hosted-blocker.md`; companion verify probe passed. Root cause later confirmed as mixed Prisma clients (different datasource paths).
- `2026-04-20` - **Hosted inbound smoke PASS after prisma-client unification** - Updated `scripts/smoke-inbound-pilot-integration.ts` and `scripts/verify-inbound-pilot-smoke.ts` to use shared `lib/db/prisma` (same datasource path as orchestration). Re-ran hosted smoke with `--tenant=sample` -> PASS; added `docs/evidence/pilot/2026-04-20-inbound-smoke-hosted.md` and `docs/evidence/pilot/2026-04-20-inbound-db-verify-hosted.md`.
- `2026-04-20` - **Pilot evidence hygiene** - Marked `docs/evidence/pilot/2026-04-20-inbound-smoke-hosted-blocker.md` as superseded by hosted PASS artifact to avoid stale handoff confusion.
- `2026-04-20` - **Pilot release-pack index added** - Added canonical handoff file `docs/evidence/pilot/2026-04-20-inbound-pilot-evidence-index.md` and linked it from pilot README, runbook, and CRM inbound GO/NO-GO row for PR/release copy-paste.
- `2026-04-20` - **Pilot PR copy pack added** - Added `docs/evidence/pilot/2026-04-20-inbound-pilot-pr-copy-pack.md` with ready-to-paste PR title/body, validation checklist, evidence links, and explicit non-goals.
- `2026-04-20` - **Pilot PR copy pack (short variant)** - Extended `docs/evidence/pilot/2026-04-20-inbound-pilot-pr-copy-pack.md` with a concise PR body option for teams that prefer minimal review templates.
- `2026-04-20` - **Pilot merge-commit suggestions added** - Appended long + short merge-commit message blocks to `docs/evidence/pilot/2026-04-20-inbound-pilot-pr-copy-pack.md` so release handoff covers both PR description and merge-commit text (scope, validation summary, non-goals, evidence index).
- `2026-04-20` - **Pilot handoff commands added** - Extended `docs/evidence/pilot/2026-04-20-inbound-pilot-pr-copy-pack.md` with copy/paste `gh` handoff commands (PR create flow + squash merge + post-merge evidence pointers) and mirrored the handoff note in `docs/evidence/pilot/2026-04-20-inbound-pilot-evidence-index.md`.
- `2026-04-20` - **Pilot PR body files materialized** - Added `docs/evidence/pilot/2026-04-20-inbound-pilot-pr-body.long.md` and `docs/evidence/pilot/2026-04-20-inbound-pilot-pr-body.short.md`, then rewired copy-pack handoff command to `gh pr create --body-file ...` for fully executable PR creation without manual paste.
- `2026-04-20` - **Pilot PR fallback documented (no `gh`)** - Added explicit browser-based PR creation fallback to `docs/evidence/pilot/2026-04-20-inbound-pilot-pr-copy-pack.md` and `docs/evidence/pilot/RUNBOOK.md`, with supporting pointer in `docs/evidence/pilot/2026-04-20-inbound-pilot-evidence-index.md` for environments where GitHub CLI is missing.
- `2026-04-20` - **Pilot squash-merge text pinned to branch commits** - Added a ready-to-paste squash merge commit block in `docs/evidence/pilot/2026-04-20-inbound-pilot-pr-copy-pack.md` explicitly mapped to branch `docs/inbound-pilot-pr-handoff` (`cda54fe3` + `10388ee3`) to reduce merge-time wording drift.
- `2026-04-20` - **Pilot browser merge checklist added** - Added a click-by-click browser merge flow to `docs/evidence/pilot/2026-04-20-inbound-pilot-pr-copy-pack.md` (PR creation, file-scope verification, squash merge, and commit-text paste), and refreshed the pinned commit list to include `7defe6d7`.
- `2026-04-20` - **Pilot post-merge verification checklist added** - Added a mini post-merge verification section to `docs/evidence/pilot/2026-04-20-inbound-pilot-pr-copy-pack.md` (main merge confirmation, GO/NO-GO/evidence pointers, runbook link checks, and closure note), and made squash-merge guidance SHA-agnostic to avoid future drift.
- `2026-04-20` - **Pilot closure comms snippets added** - Added ready-to-paste Slack and Jira closeout snippets to `docs/evidence/pilot/2026-04-20-inbound-pilot-pr-copy-pack.md` so release communication stays aligned with canonical evidence and explicit non-goals.

- `2026-04-21` - **Public onboarding CTA routing + signup module payload** - Standardized public CTAs to industry-first onboarding (`/#industry-selector`), hardened app-store CTAs (anchors + signup deep links), fixed module marketing links/icons, added explainer video modal shell (`NEXT_PUBLIC_EXPLAINER_VIDEO_URL`), and ensured `/signup` + `/register` send `planType` + `selectedModules` to auth registration.
- `2026-04-22` - **Checklist merge conflict cleanup (unblocks deploy)** - Resolved `docs/PAYAID_V3_PENDING_ITEMS_PRIORITY_CHECKLIST.md` stash/merge markers by taking upstream copy so commits can ship cleanly again.
- `2026-04-22` - **Dev signup cold-compile relief** - Lazy-loaded heavy server deps inside `POST /api/auth/register` (`apps/dashboard/app/api/auth/register/route.ts`) to reduce first-hit webpack compile time during local `next dev`.
- `2026-04-22` - **Navigation hop removal for faster route changes** - Updated `lib/utils/dashboard-url.ts` to resolve legacy `/dashboard/...` links directly to decoupled module routes via `getRedirectForDashboardPath(...)`, avoiding extra client-side redirect hops on every navigation.
- `2026-04-22` - **Voice preview 404 removed** - Added missing `GET /api/voice/preview` route (`apps/dashboard/app/api/voice/preview/route.ts`) with auth + TTS response so voice-agent wizard preview no longer compiles `/_not-found` on each click.
- `2026-04-22` - **Voice preview first-click warmup added** - Added `HEAD /api/voice/preview` health path and one-time warmup call in `voice-agents/[tenantId]/create` to pre-warm auth/route handling before users press Preview.
- `2026-04-22` - **Voice preview warmup regression fix** - Defined missing `DEFAULT_PREVIEW_TEXT` constant in `voice-agents/[tenantId]/create/page.tsx` to resolve client runtime `ReferenceError` on page mount.
- `2026-04-22` - **Voice preview fallback for local TTS outages** - Updated `voice-agents/[tenantId]/create/page.tsx` to use browser `speechSynthesis` fallback when `/api/voice/preview` fails (e.g., VEXYL/Coqui unavailable), and surface a user-visible message instead of silent no-op.
- `2026-04-22` - **Voice preview set to backend-only experience** - Removed browser `speechSynthesis` fallback from `voice-agents/[tenantId]/create/page.tsx`; preview now depends only on `/api/voice/preview` and shows a clear connectivity error when TTS backends are unavailable.
- `2026-04-22` - **VEXYL TTS route auto-detection for real preview** - Updated `lib/voice-agent/vexyl-tts.ts` to auto-try common backend routes (`/tts`, `/api/tts`) when `VEXYL_TTS_PATH` is not set, fixing preview failures on voice-gateway setups that do not expose `/tts`.
- `2026-04-22` - **Voice preview now prefers Sarvam real TTS** - Updated `GET /api/voice/preview` to use `sarvamTts(...)` first when configured (returns MP3), then fallback to existing TTS chain, improving real preview reliability when local VEXYL/Coqui endpoints are unavailable.
- `2026-04-22` - **Login timeout UX hardened for DB outages** - Added a fast DB socket precheck in `apps/dashboard/app/api/auth/login/route.ts` so unreachable database scenarios return a clear `503 Database unavailable` response immediately instead of waiting for long login timeouts.
- `2026-04-22` - **Home load + tenant URL personalization update** - Added tenant `slug` through auth payload/store (`/api/auth/login`, `/api/auth/me`, `lib/stores/auth.ts`), updated `/home` and `/home/[tenantId]` to accept both id and slug with canonical redirect to slug URLs, and redirected post-login home navigation to `/home/{tenantSlug}` when available.
- `2026-04-22` - **Voice label/speaker sync hardening (male/female tones)** - Added deterministic app-voice-id to Sarvam speaker mapping in `lib/voice-agent/sarvam.ts` (for `arjun/divya/priya/rahul` variants) so UI labels like `Male Formal` / `Female Calm` consistently resolve to matching backend voice identities.
- `2026-04-22` - **Voice picker names switched to actual speakers** - Updated voice-agent create/new UI label text to show real speaker names (`Abhilash`, `Rahul`, `Aditya`, `Vidya`, `Anushka`, `Manisha`, `Priya`) while keeping existing voice IDs stable for backward compatibility.
- `2026-04-22` - **Studio voice picker aligned to speaker names** - Updated `voice-agents/[tenantId]/studio/page.tsx` voice field from free-text input to a mapped speaker dropdown using the same real speaker labels as create/new, with custom-id fallback option for legacy agents.
- `2026-04-22` - **Voice options centralized to shared source-of-truth** - Added `lib/voice-agent/voice-options.ts` (`VOICE_OPTIONS`, `DEFAULT_VOICE_ID`, `isKnownVoiceId`) and refactored create/new/studio pages to import shared voice labels/ids, preventing future naming drift.
- `2026-04-22` - **Voice preview backend error surfacing + strict Sarvam path** - Updated `/api/voice/preview` to use strict Sarvam mode when configured (no local fallback in that mode) and surfaced backend error text in create-page preview UI, replacing generic connectivity-only messaging.
- `2026-04-22` - **Sarvam bulbul:v3 speaker compatibility sync** - Replaced invalid speaker mappings (`abhilash/vidya/anushka/manisha`) with `bulbul:v3`-supported speakers (`ashutosh/ritu/neha/kavya`) in `lib/voice-agent/sarvam.ts`, and updated shared picker labels in `lib/voice-agent/voice-options.ts` to match.
- `2026-04-22` - **Password recovery + one-time tenant admin reset path added** - Added user-facing forgot/reset password flow (`/forgot-password`, `/reset-password`, `/api/auth/forgot-password`, `/api/auth/reset-password`) with single-use 30-minute token handling via `lib/auth/password-reset.ts`; added a locked one-time admin reset-link endpoint for Kennt Cement (`/api/admin/kennt-cement/one-time-reset-link`) gated by `ADMIN_UTILITY_SECRET` and `KENNT_CEMENT_ONE_TIME_RESET_KEY`.
- `2026-04-22` - **Trial-expiry UX guardrails added** - Added subscription hard-guard in `proxy.ts` to redirect `billingStatus=payment_required` users to billing/checkout paths, and added global trial/expiry banner + upgrade CTA in `components/modules/ModuleTopBar.tsx` using `/api/billing/trial-status`.
- `2026-04-21` - **Supabase public-table hardening script added** - Added `prisma/migrations/harden_supabase_public_access.sql` to enforce RLS on all `public` tables, revoke `anon/authenticated` table privileges for sensitive-column tables, and apply service-role-only policies as defense-in-depth for Supabase API exposure.
- `2026-04-21` - **Supabase hardening verification + controlled allowlist added** - Added `prisma/migrations/verify_supabase_public_hardening.sql` (post-apply checks), `prisma/migrations/allowlist_supabase_api_tables.sql` (deny-by-default exceptions template), and `docs/SUPABASE_SECURITY_HARDENING_RUNBOOK.md` (production rollout/rollback-safe procedure).
- `2026-04-21` - **Supabase RLS no-policy lint closure added** - Added `prisma/migrations/fix_rls_enabled_no_policy.sql` to auto-create `service_role` policies on RLS-enabled public tables that currently have zero policies, closing Security Advisor `rls_enabled_no_policy` findings without reopening anon/authenticated access.
- `2026-04-21` - **Supabase targeted no-policy fallback added** - Added `prisma/migrations/fix_rls_enabled_no_policy_targeted.sql` to force-create `service_role` policies for the six persistently flagged tables (`Inbound*`, `OutreachAutomation`, `SpecialistActivityLog`) when generic closure still leaves INFO findings.
- `2026-04-21` - **Launch checklist DB security gate added** - Updated `docs/LAUNCH_CHECKLIST.md` to include a mandatory Supabase DB security gate (harden/fix/verify SQL sequence + Advisor clean pass criteria) under Security and Pre-Launch Code & Infrastructure checks.
- `2026-04-21` - **Supabase security evidence path standardized** - Updated `docs/LAUNCH_CHECKLIST.md` DB security gate with explicit archive location `docs/evidence/security/supabase/YYYY-MM-DD/` and required artifacts (SQL execution transcript, verification output, Advisor pass screenshot/export).
- `2026-04-21` - **Supabase evidence folder scaffolded** - Added `docs/evidence/security/supabase/2026-04-21/README.md` with required artifact filenames and a copy/paste template for environment, scripts executed, and pass/fail outcomes.
- `2026-04-21` - **Supabase evidence files pre-created** - Added `01-harden-sql-execution.md`, `02-verify-query-output.md`, and `03-security-advisor-clean.md` under `docs/evidence/security/supabase/2026-04-21/` so operators can paste execution proof without manual file setup.
- `2026-04-22` - **Voice preview samples upgraded to sentence-level style demos** - Updated `voice-agents/[tenantId]/create/page.tsx` language previews from single words to full sentences per language, replaced voice preview text fallback (`Ravi`) with style-aware sample sentences (formal/calm/warm), and aligned `/api/voice/preview` default text to a full sentence for consistent preview behavior.
- `2026-04-22` - **Voice style previews localized for all supported languages** - Extended style-aware preview sentences in `voice-agents/[tenantId]/create/page.tsx` for Tamil, Telugu, Kannada, and Marathi (in addition to Hindi/English) so Calm/Warm/Formal differences are demonstrable regardless of selected language.
- `2026-04-22` - **Voice previews switched to generic native-script lines** - Removed brand-specific `PayAid` wording from voice preview samples and replaced transliterated text with native scripts (Hindi/Tamil/Telugu/Kannada/Marathi) plus neutral style sentences in `voice-agents/[tenantId]/create/page.tsx`; aligned `/api/voice/preview` default text to native-script generic wording for more natural pronunciation.
- `2026-04-22` - **Sarvam speaker routing split by language + style** - Refactored `lib/voice-agent/sarvam.ts` speaker resolution to be language-aware (separate Hindi-native and English-oriented default pools), preserve style (`formal/calm/warm`) and inferred gender from app `voiceId`, and apply per-language native defaults (`hi/en/ta/te/kn/mr`) before fallback.
- `2026-04-22` - **In-call language switch for Twilio speech flow** - Updated `apps/voice/app/api/v1/voice-agents/twilio/speech-handler/route.ts` to detect caller requests like "speak in Hindi/English", acknowledge the switch immediately, persist `languageUsed` on the active call, and continue subsequent STT gather + LLM/TTS in the switched language.
- `2026-04-22` - **All supported call languages now switchable in-session** - Added shared `lib/voice-agent/language-switch.ts` utilities (intent detection, per-language switch acknowledgements, Twilio locale mapping, language normalization) for `hi/en/ta/te/kn/mr/gu/pa/bn/ml`; wired into both `lib/voice-agent/orchestrator.ts` (non-Twilio calls) and Twilio speech handler; updated call-process route to prefer persisted `languageUsed` for multi-turn consistency.
- `2026-04-22` - **Voice preview deterministic speaker/tone lock** - Fixed preview inconsistency by inferring `voiceTone` from `voiceId` in `/api/voice/preview` and updating `lib/voice-agent/tts.ts` to derive VEXYL style from `voiceId` (instead of forcing `calm`), preventing repeated preview clicks from drifting speaker/tone when selection is unchanged.
- `2026-04-22` - **Daily conversational language tuning (no archaic phrasing)** - Rewrote style preview sentences in `voice-agents/[tenantId]/create/page.tsx` to use modern day-to-day wording, simplified multilingual language-switch acknowledgements in `lib/voice-agent/language-switch.ts`, and added explicit runtime prompt instructions in `lib/voice-agent/orchestrator.ts` and Twilio speech handler to avoid bookish/old-fashioned vocabulary in live responses.
- `2026-04-22` - **Per-agent conversational mode toggle added** - Added `conversationStyle` (`casual` / `neutral` / `professional`) selection in create/new/studio voice-agent flows, persisted in `workflow`, and enforced in runtime prompt builders (`lib/voice-agent/orchestrator.ts`, `apps/voice/.../twilio/speech-handler/route.ts`) so agents consistently speak in the configured day-to-day style.
- `2026-04-22` - **Terminology registry v1 + CRM pilot labels** - Added canonical terminology layer (`lib/terminology/terms.ts`, `industry-presets.ts`, `get-term.ts`, `get-plural-term.ts`, `tenant-overrides.ts`, `use-terms.ts`) with Industry+Tenant fallback semantics, added tests (`lib/terminology/__tests__/get-term.test.ts`), extended auth tenant shape with optional `industry`, and migrated CRM stage labels in `components/crm/LeadsKanban.tsx` + `components/crm/StageBadge.tsx` to `useTerms()` while keeping canonical backend stages (`prospect|contact|customer`) unchanged.
- `2026-04-22` - **Terminology rollout wave 2 (high-visibility CRM surfaces)** - Extended terminology resolver usage to `components/crm/EmptyState.tsx`, `components/crm/DealsKanban.tsx`, and `components/crm/PipelineHealthDashboard.tsx` so deal/pipeline/contact labels and empty-state copy render via `useTerms()` + `pluralTerm()` (industry-aware UI terms, unchanged backend canonical keys).
- `2026-04-22` - **Operational 5-skill project pack scaffolded + specialist-flow integration** - Added `docs/ai/agents/` skill set (`repo-audit-implementation-verification`, `pilot-release-gate-go-no-go`, `smoke-test-source-path-verification`, `prisma-migration-discipline`, `tenant-scope-multi-tenant-guardrails`), created `docs/ai/internal-workflow.md`, and updated `docs/ai/specialist-execution-template.md` with skill overlay, per-skill evidence capture, and ship-gate check.
- `2026-04-22` - **First live skill-pack ticket run executed (Queue #5 / Day 4 closure)** - Added `docs/ai/specialist-skillpack-first-live-run-2026-04-22-queue5.md` with Product/CRM/Code-Review outputs, selected-skill audit table, gate-card decision (**NO-GO**), and ordered closure steps; surfaced missing Day 4 runbook/evidence references as primary blockers.
- `2026-04-22` - **Day 4 closure docs restored + skill-pack gate rerun** - Recreated `docs/CRM_GA_SOLO_T04_QA_RUNBOOK.md`, `docs/CRM_GA_SOLO_T04_PREP_DISCOVERY.md`, `docs/CRM_GA_SOLO_T04_SUPPORT_MERGE_ERRORS.md`, and restored evidence path `docs/evidence/closure/2026-04-20T22-15-00-000Z-crm-day4-merge-guard-tests.md`; added rerun artifact `docs/ai/specialist-skillpack-rerun-2026-04-22-queue5.md` confirming missing-file blocker is cleared and remaining blockers are manual A-D checks + Product signoff.
- `2026-04-22` - **Day 4 runbook execution worksheet prefilled** - Added a dated fill-ready execution worksheet and paste template to `docs/CRM_GA_SOLO_T04_QA_RUNBOOK.md` (planned artifact path + A/B/C/D capture fields + product signoff block) to accelerate Queue #5 manual closure evidence capture.
- `2026-04-22` - **Day 4 runtime evidence shell created** - Added `docs/evidence/closure/2026-04-22-crm-day4-runtime-checks.md` as a preformatted A-D + product-signoff artifact template so Queue #5 execution results can be captured in one pass and synced directly to launch/closure trackers.
- `2026-04-22` - **Day 4 automation rerun attempt logged as environment blocker** - Attempted `test:crm:merge-key`, chained `merge-key` + `merge-guard`, and `collect:crm-closure-blockers:tests-only`; all hung before Jest summaries in this shell session. Recorded attempt details in `docs/evidence/closure/2026-04-22-crm-day4-runtime-checks.md` and synced queue-note status in `docs/CRM_GA_CLOSURE_EXECUTION_LOG.md`.
- `2026-04-22` - **Day 4 hosted runtime verification executed (Queue #5)** - Added automated hosted run output to `docs/evidence/closure/2026-04-22-crm-day4-runtime-checks.md` via `scripts/run-day4-merge-checks.mjs` (`BASE_URL=https://payaid-v3.vercel.app`, `admin@demo.com`), yielding pass set A1-A4/B1-B3/C1/D1/D2/D4/D5 and narrowing blockers to D3 (`SKIPPED_NO_DUPLICATE_PAIR`), D6 bypass merge runtime `500`, and Product signoff.
- `2026-04-22` - **Day 4 D6 merge timeout mitigation implemented** - Updated `lib/data-quality/duplicate-detector.ts` to run merge workflow with explicit Prisma interactive transaction options (`maxWait: 10000`, `timeout: 60000`) to prevent hosted long-merge flows from expiring transaction context (`Transaction not found`); launch/closure docs now mark this as pending deploy+rereun verification.
- `2026-04-22` - **Day 4 post-fix local verification attempt recorded** - Attempted rerun of `scripts/run-day4-merge-checks.mjs` against `http://127.0.0.1:3000` (artifact target `docs/evidence/closure/2026-04-22-crm-day4-runtime-checks-local-postfix.md`), but run failed with `ECONNREFUSED`; noted in closure evidence/log and kept D6 fix in pending deploy+rerun state.
- `2026-04-23` - **Day 4 deploy-capable rerun attempted; deployment queue-blocked** - Hardened checker script timeout handling (`scripts/run-day4-merge-checks.mjs` using `DAY4_REQUEST_TIMEOUT_MS`) and initiated production deploy (`dashboard-4m7ywsoo5-payaid-projects-a67c6b27.vercel.app`), but deployment remained `Queued` across repeated `vercel inspect --logs` checks; recorded blocker in Day 4 evidence/log and kept D6 verification pending deploy readiness.
- `2026-04-23` - **Day 4 local post-fix rerun succeeded (D6 cleared)** - Re-ran `scripts/run-day4-merge-checks.mjs` against local dev with extended timeout (`DAY4_REQUEST_TIMEOUT_MS=240000`) and wrote `docs/evidence/closure/2026-04-23-crm-day4-runtime-checks-local-postfix.md`; D6 now passes (`200`), leaving Queue #5 blockers narrowed to D3 duplicate-pair precondition and Product signoff.
- `2026-04-23` - **Day 4 D3 N/A-candidate policy + evidence path added** - Extended `scripts/run-day4-merge-checks.mjs` with `DAY4_D3_ALLOW_NA=1` mode and generated `docs/evidence/closure/2026-04-23-crm-day4-runtime-checks-local-na-candidate.md`; updated `docs/CRM_GA_SOLO_T04_QA_RUNBOOK.md` with explicit D3 N/A eligibility criteria + paste-ready Product signoff block, and synced launch/closure/specialist gate docs so Queue #5 can close on Product+QA acceptance without synthetic duplicate seeding.
- `2026-04-23` - **Day 4 Product signoff draft prefilled for final approval** - Added a prefilled Product signoff draft block to `docs/CRM_GA_SOLO_T04_QA_RUNBOOK.md` (`Decision: Pending confirmation`, evidence-linked risk note), and synced `docs/LAUNCH_CHECKLIST.md` + `docs/CRM_GA_CLOSURE_EXECUTION_LOG.md` so Queue #5 is explicitly “awaiting final Product owner/date/decision entry.”
- `2026-04-23` - **Day 4 Queue #5 closure finalized** - Captured Product owner/date/decision approval in `docs/CRM_GA_SOLO_T04_QA_RUNBOOK.md`, accepted D3 as N/A under the runbook eligibility rule with linked evidence, and marked Day 4 rows + Queue #5 as **Completed** in `docs/LAUNCH_CHECKLIST.md` and `docs/CRM_GA_CLOSURE_EXECUTION_LOG.md`.
- `2026-04-23` - **Day 5 Queue #6 execution pack staged** - Restored missing Day 5 runbook references by adding `docs/CRM_GA_SOLO_T05_ROLE_MATRIX_RUNBOOK.md` and `docs/CRM_GA_SOLO_T05_AUDIT_VERIFICATION_RUNBOOK.md`, created unified evidence worksheet `docs/evidence/closure/2026-04-23-crm-day5-rbac-audit-runtime-checks.md`, and synced Queue #6 status to **In Progress** in launch + closure trackers.
- `2026-04-23` - **Day 5 Queue #6 first execution attempt logged** - Ran `npm run test:crm:rbac` (pass) and retried `npm run release:gate:crm-audit` twice (timeout/fail) with artifacts `docs/evidence/release-gates/2026-04-23T09-47-14-301Z-crm-audit-gate.json` and `docs/evidence/release-gates/2026-04-23T09-52-59-193Z-crm-audit-gate.json`; kept Queue #6 **In Progress** pending stable signed-in role matrix + audit-event runtime verification.
- `2026-04-23` - **Day 5 Queue #6 stabilization retry logged** - Patched `tests/e2e/crm-audit/crm-audit.spec.ts` to improve 360 recovery (reload/list fallback + bounded timeout), reran `release:gate:crm-audit` with extended timeout (`CRM_AUDIT_TIMEOUT_MS=1200000`), and captured continued failure artifact `docs/evidence/release-gates/2026-04-23T10-26-16-754Z-crm-audit-gate.json`; Queue #6 remains **In Progress** pending stable runtime path.
- `2026-04-23` - **Day 5 hosted role-matrix verification completed (Queue #6)** - Added targeted automation `scripts/run-day5-role-matrix-checks.mjs`, set hosted test-user roles (`admin/manager/rep/read_only`) via admin API, and captured green matrix artifact `docs/evidence/closure/2026-04-23T13-06-22-219Z-crm-day5-role-matrix-automation.json` (60 pass / 0 fail).
- `2026-04-23` - **Day 5 hosted audit verification narrowed to single blocker** - Added `scripts/run-day5-audit-verification-checks.mjs` and captured hosted artifact `docs/evidence/closure/2026-04-23T13-12-01-757Z-crm-day5-audit-verification-automation.json` (A1-A3, A5-A7 pass; A4 fail on `contacts/mass-transfer` FK `500`), then fixed route assignment logic in `apps/dashboard/app/api/crm/contacts/mass-transfer/route.ts` to use `SalesRep.id`; Queue #6 remains open pending deploy+rereun of A4.
- `2026-04-22` - **Trial-expiry proxy guard Edge decode fix** - Updated `proxy.ts` `safeDecodeToken(...)` to use Edge-safe JWT payload decoding for `billingStatus`/tenant routing checks, restoring `payment_required` hard-redirect behavior on production middleware paths.
- `2026-04-22` - **Terminology rollout wave 3 (CRM command/action surfaces)** - Extended `useTerms()` usage in `components/crm/CommandPalette.tsx`, `QuickActionsPanel.tsx`, `ContactList.tsx`, and `StagePromotionButton.tsx` so contact/deal/customer labels in command navigation, quick actions, list headings, loading/confirm text, and stage-promotion CTA copy follow industry-aware terminology without changing canonical stage keys or API payload enums.
- `2026-04-22` - **Terminology rollout wave 4 (CRM contextual actions + bulk dialogs)** - Extended terminology-aware copy to `components/crm/QuickActionsPanel.tsx` dialog descriptions, `components/crm/RowActionsMenu.tsx` (convert/disqualify/create/delete confirmations), and `components/crm/BulkOperations.tsx` dialog titles/descriptions plus lead/customer option labels, preserving canonical payload values while adapting UI terminology by industry.
- `2026-04-22` - **Terminology rollout wave 5 (pipeline config + residual action polish)** - Added terminology-aware pipeline wording in `components/crm/DealPipelineCustomizer.tsx` (`Pipeline Stages`, `Save Pipeline Configuration`) and completed action-surface terminology polish in `QuickActionsPanel`, `RowActionsMenu`, and `BulkOperations` to keep CRM commands/dialogs consistent with industry vocabulary overlays.
- `2026-04-22` - **Terminology rollout wave 6 (analytics + AI + contact detail surfaces)** - Extended `useTerms()` usage in `components/crm/CRMAICommandCenter.tsx`, `AdvancedAnalytics.tsx`, `DealRotWidget.tsx`, and `components/crm/contact/ContactInfoSection.tsx` so pipeline/deal/lead/contact/account labels in AI prompts, analytics descriptions, deal-rot summaries, and contact sidebar sections follow industry-specific terminology without changing backend identifiers.
- `2026-04-22` - **Terminology rollout wave 7 (automation + health + scoring + template analytics)** - Added terminology-aware wording across `components/crm/sales-automation/SalesAutomationCommandCenter.tsx`, `DealHealthCard.tsx`, `LeadScoreCard.tsx`, and `TemplateAnalytics.tsx` for lead/deal/pipeline/account labels in KPI cards, queue tabs, health headers, scoring badges, and template-performance sections while preserving canonical API keys and stage identifiers.
- `2026-04-22` - **Terminology rollout wave 8 (automation analytics alignment)** - Extended terminology coverage in `components/crm/sales-automation/SalesAutomationCommandCenter.tsx` and `SalesAutomationAnalyticsTab.tsx` so high-intent queue narratives and enrolled/pipeline analytics labels resolve through canonical term helpers for industry-aware CRM wording.
- `2026-04-22` - **Terminology rollout wave 9 (search + nurture + automation + insights copy)** - Applied terminology-aware CRM wording in `components/crm/UniversalSearch.tsx`, `WorkflowAutomationPanel.tsx`, `LeadNurtureFlow.tsx`, and `CustomerInsights.tsx` for search placeholders/type labels, lead/deal sample automation copy, nurture headers/score labels, and customer insight titles.
- `2026-04-22` - **Terminology rollout wave 10 (forecasting + templates + account/prospect panels)** - Added canonical-term-driven labels in `components/crm/ScenarioPlanner.tsx`, `TemplateSelector.tsx`, `AccountHealthWidget.tsx`, `RevenueForecast.tsx`, and `components/crm/contact/ProspectAIPanel.tsx` so deal/lead/contact/account/customer wording in scenario controls, template previews, forecasts, and AI fit panels adapts by industry while API contracts remain unchanged.
- `2026-04-22` - **Terminology rollout wave 11 (contact/deal timelines + task quick actions)** - Extended term-resolution usage to `components/crm/deal/DealTimeline.tsx`, `components/crm/contact/ContactTimeline.tsx`, `ContactInfoTimelineCard.tsx`, `QuickActionsCard.tsx`, and `components/crm/tasks/TaskCard.tsx` to replace hardcoded deal/contact/task/pipeline headings and action labels while preserving existing routing and timeline behavior.
- `2026-04-22` - **Terminology rollout wave 12 (next-action + intelligence + task sidebar + comms filters)** - Added dynamic term rendering in `components/crm/contact/NextBestActionCard.tsx`, `ContactIntelligenceCard.tsx`, `components/crm/tasks/TasksSidebar.tsx`, and `components/crm/CommunicationTimeline.tsx` so lead/contact/task labels in coaching suggestions, intelligence summaries, sidebar headings, and communication filters respect tenant industry vocabulary.
- `2026-04-22` - **Terminology rollout wave 13 (AI enrichment + dashboard widget labels)** - Added terminology-aware CRM wording in `components/crm/contact/AIAssistCard.tsx`, `ContactAIEnrichmentPanel.tsx`, `components/crm/ContactEnrichment.tsx`, and `DashboardCustomizer.tsx` to align enrichment CTAs, contact copy, and widget titles with canonical term resolution for industry-specific UI language.
- `2026-04-22` - **Terminology rollout wave 14 (contact-360 duplicate/insight language hardening)** - Extended terminology coverage in `components/crm/contact/Contact360RollupSections.tsx` for duplicate-merge confirmations/buttons and customer/contact phrasing in insight and related-contact labels so cross-record merge guidance and contact-360 narratives stay industry-aware.
- `2026-04-22` - **Terminology rollout wave 15 (CRM terminology v1 completion sweep)** - Finalized remaining user-visible contact/lead/task phrasing in `components/crm/contact/ContactIntelligenceCard.tsx`, `AIAssistCard.tsx`, `ContactAIEnrichmentPanel.tsx`, and `NextBestActionCard.tsx`, bringing high-visibility CRM UI copy under canonical term resolution with industry-aware labels while keeping template variable keys and backend identifiers stable.
- `2026-04-23` - **Terminology QA pass (industry snapshot checks)** - Expanded `lib/terminology/__tests__/get-term.test.ts` with healthcare and retail snapshot expectations for singular/plural lead/contact/deal plus pipeline labels to validate resolver output used by CRM UI. Jest invocation was attempted for this spec path but hung in the local shell environment (known issue), so verification was completed via static assertions + lint-clean checks.
- `2026-04-23` - **Terminology QA extension (table-driven alias coverage)** - Strengthened the table-driven terminology test in `lib/terminology/__tests__/get-term.test.ts` to verify alias normalization plus lead/contact/deal/pipeline singular+plural outputs for `real_estate`, `service_businesses`, and `financial`, improving regression coverage for industry-label fallback behavior.
- `2026-04-23` - **Terminology QA cleanup (shared assertion helper)** - Refactored `lib/terminology/__tests__/get-term.test.ts` with a reusable `expectIndustryTerms(...)` helper and an explicit `IndustryExpectation` shape so healthcare/retail snapshots and alias table-driven cases share the same canonical assertion path, reducing repetition and improving maintainability for future industry additions.
- `2026-04-23` - **Terminology QA consolidation (single table-driven snapshot suite)** - Consolidated healthcare/retail snapshot assertions and alias-normalization coverage into one table-driven industry suite in `lib/terminology/__tests__/get-term.test.ts`, using optional per-case normalization checks plus shared `expectIndustryTerms(...)` assertions so adding a new industry snapshot is now a single object entry.
- `2026-04-23` - **Terminology QA fallback guard (unknown industry defaults)** - Added a negative-case table in `lib/terminology/__tests__/get-term.test.ts` to assert canonical term/plural fallbacks and `normalizeIndustryKey(...) === null` for undefined/null/empty and unknown industry inputs, preventing regressions that could leak invalid industry values into UI labels.
- `2026-04-23` - **Terminology QA precedence guard (tenant > industry > default)** - Added a table-driven precedence test in `lib/terminology/__tests__/get-term.test.ts` to verify singular/plural resolver order across representative cases: tenant overrides taking priority over industry presets, industry presets applying when tenant overrides are absent, and canonical defaults applying when both are absent.
- `2026-04-22` - **Dashboard runtime trial guard activation fix** - Added trial-expiry redirect guard directly in `apps/dashboard/proxy.ts` (active dashboard runtime proxy), with Edge-safe token decode + expanded matcher coverage (`dashboard/home/crm/hr/sales/finance/marketing/projects/settings`) so `billingStatus=payment_required` redirects to billing paths in production.
- `2026-04-22` - **Business OS taxonomy baseline + sync matrix v1** - Added canonical taxonomy mapping in `lib/taxonomy/business-os-taxonomy.ts` (11 top-level suites, platform capabilities, workspace tools, industry overlays, and module ownership map), aligned registry labels in `lib/config/payaid-modules.config.ts` + `lib/modules.config.ts` + `lib/moduleMarketing.ts` (AI Workspace, HR & Workforce, Projects & Service, Automation, Documents & Contracts), refreshed landing taxonomy language in `components/landing/LandingPage.tsx`, and published architecture reference `docs/PAYAID_V3_BUSINESS_OS_TAXONOMY_AND_SYNC_MATRIX.md` (domain ownership + event schema + priority sync matrix + fallback rules).
- `2026-04-22` - **Business OS phase 2 alignment (nav + onboarding + public surfaces)** - Updated onboarding defaults in `lib/onboarding/signup-recommendations.ts` to prioritize core suite ownership and reduce productivity-first overlap, refreshed module tier labels/grouping in `lib/modules/module-tiers.ts` and top-nav More Apps ordering in `components/ModuleSwitcher.tsx`, and aligned public catalog copy in `apps/dashboard/app/features/page.tsx`, `apps/dashboard/app/modules/page.tsx`, `apps/dashboard/app/modules/[slug]/page.tsx`, and `components/Navigation/ModuleSwitcher.tsx` to the Business OS hierarchy (suites vs capabilities/tools).
- `2026-04-22` - **Business OS phase 3 pruning (public catalog overlap reduction)** - Added curated public catalog sections in `apps/dashboard/app/modules/page.tsx` (Core Business Suites, Industry Solutions, Recommended Starter Bundles), introduced explicit marketing catalog filters and canonical suite slugs in `lib/moduleMarketing.ts` (including `operations`, `support`, `documents-contracts`, `automation`, `ai-workspace`), normalized module-id to marketing-slug routing toward suite-first pages, and pruned `apps/dashboard/app/features/page.tsx` to suite-representative module IDs to remove duplicate AI/tool surfaces from primary public positioning.
- `2026-04-22` - **Business OS phase 4 (in-app switcher primary-surface hardening)** - Updated internal switcher hierarchy to prioritize top-level suites and demote non-suite surfaces: rebalanced tier ownership in `lib/modules/module-tiers.ts` (suite-first Tier 1), refreshed `components/modules/ModuleSwitcher.tsx` suite header copy, updated `components/ModuleSwitcher.tsx` labels (`Business Suites`, `Capabilities & Tools`), added taxonomy-based surface classification in `components/Navigation/ModuleSwitcher.tsx` (Business Suites vs Platform Capabilities vs AI Workspace Tools vs Workspace Tools vs Other Features), and renamed secondary group labels in `lib/config/payaid-modules.config.ts` to explicit capability/tool terminology.
- `2026-04-22` - **Business OS phase 5 (module-enabled guard consistency)** - Tightened module visibility enforcement in `lib/tenant/module-license-filter.ts` with explicit alias ownership for secondary surfaces (automation/support/docs/analytics extensions/workspace tools) so they resolve to canonical licensed modules, updated `apps/dashboard/app/api/modules/switch/route.ts` to enforce alias-aware tenant module checks via `isModuleListedForTenantLicense(...)`, and expanded `apps/dashboard/proxy.ts` matcher coverage for suite/capability routes (`inventory`, `analytics`, `workflow-automation`, `ai-studio`, `support`, `contracts`, `appointments`, `communication`, `productivity`) so trial-expiry subscription guard behavior is consistent across the standardized taxonomy.
- `2026-04-22` - **Business OS phase 6 (admin/settings guard + alias-test baseline)** - Updated tenant module-admin API `apps/dashboard/app/api/admin/tenants/[tenantId]/modules/route.ts` to return canonical license-module definitions aligned to suite taxonomy and to enforce alias-aware PUT writes via `resolveLicenseModuleId(...)`; exported canonical alias resolver from `lib/tenant/module-license-filter.ts`; added targeted regression coverage in `lib/tenant/__tests__/module-license-filter.test.ts` for AI/support/workflow/appointments/analytics alias behavior and always-visible route rules.
- `2026-04-22` - **Business OS phase 7 (user-module assignment canonicalization + tests)** - Added `lib/tenant/user-module-access.ts` to centralize licensed-module normalization and alias-aware assignment mapping (`normalizeRequestedModuleAccess`, `buildCanonicalModuleAccessMap`), refactored `apps/dashboard/app/api/admin/users/[userId]/modules/route.ts` to return canonical module access maps and rewrite stored `moduleAccess` rows using canonical module IDs only, and added regression coverage in `lib/tenant/__tests__/user-module-access.test.ts` for alias normalization/filtering and forced-admin map behavior.
- `2026-04-22` - **Business OS phase 8 (module metadata API canonical shape)** - Added shared canonical license module catalog `lib/taxonomy/license-module-catalog.ts` and reused it in tenant module-admin API (`apps/dashboard/app/api/admin/tenants/[tenantId]/modules/route.ts`) to prevent taxonomy drift; refactored `/api/modules` (`apps/dashboard/app/api/modules/route.ts`) to return canonical suite-first metadata (`canonical.{all,suites,features,capabilities}` + taxonomy block) while preserving backward-compatible keys; normalized selected module ids in industry module configuration routes (`apps/dashboard/app/api/industries/[industry]/modules/route.ts`, `apps/dashboard/app/api/industries/custom/modules/route.ts`) using canonical alias resolver; extended alias tests in `lib/tenant/__tests__/module-license-filter.test.ts` for `normalizeSelectedModuleIds(...)`.
- `2026-04-22` - **Business OS phase 9 (industry recommendation/API canonicalization)** - Migrated industry recommendation internals in `lib/industries/module-config.ts` to canonical licensed-module normalization (suite-first base foundation modules + alias-safe enable/disable checks), upgraded `getRecommendedModulesForIndustry(...)` to return canonical fields (`suites`, `capabilities`, `optionalSuites`, `canonical`) with explicit `compatibility.deprecated` aliases (`coreModules`, `industryPacks`, `optionalModules`), updated `/api/industries/[industry]/modules` GET/POST and `/api/industries/custom/modules` POST to emit canonical response blocks plus compatibility aliases, and updated `/api/ai/analyze-industry` to enforce canonical suite module recommendations and return normalized fallback payloads with canonical/compatibility metadata.
- `2026-04-22` - **Business OS phase 10 (canonical-only API mode flag)** - Added `lib/taxonomy/canonical-api-mode.ts` with `CANONICAL_MODULE_API_ONLY` gating and updated `/api/modules`, `/api/industries/[industry]/modules` (GET/POST), `/api/industries/custom/modules` (POST), and `/api/ai/analyze-industry` so deprecated compatibility payload fields are included only when legacy mode is enabled; canonical blocks remain default, enabling controlled consumer cutover without response ambiguity.
- `2026-04-22` - **Business OS phase 11 (canonical cutover guardrails + test/docs)** - Added env-flag regression coverage in `lib/taxonomy/__tests__/canonical-api-mode.test.ts` for `CANONICAL_MODULE_API_ONLY` behavior, published operational cutover+rollback guidance in `docs/CANONICAL_MODULE_API_CUTOVER_RUNBOOK.md`, and linked the runbook from `docs/LAUNCH_CHECKLIST.md` execution artifacts so canonical-only rollout can be validated and reversed safely.
- `2026-04-22` - **Business OS phase 12 (endpoint contract tests + consumer readiness checklist)** - Added contract-gating tests for canonical vs legacy response shapes in `apps/dashboard/app/api/modules/__tests__/route.test.ts`, `apps/dashboard/app/api/industries/[industry]/modules/__tests__/route.test.ts`, and `apps/dashboard/app/api/ai/analyze-industry/__tests__/route.test.ts`; published `docs/CANONICAL_MODULE_API_CONSUMER_READINESS_CHECKLIST.md` with per-surface cutover checks and linked it from `docs/LAUNCH_CHECKLIST.md` to harden pre-flag consumer validation.
- `2026-04-22` - **Business OS phase 13 (CI-safe contract evidence script + rollout wiring)** - Added `scripts/check-canonical-module-api-contract.mjs` with npm command `check:canonical-module-api-contract` to emit pass/fail JSON+Markdown artifacts in `docs/evidence/closure/` without Jest runtime dependency; updated `docs/CANONICAL_MODULE_API_CUTOVER_RUNBOOK.md` with command-driven evidence steps and linked this command into `docs/LAUNCH_CHECKLIST.md` execution artifacts for pre/post flag-cutover validation.
- `2026-04-22` - **Business OS phase 14 (release-gate enforcement for canonical contract)** - Integrated `check:canonical-module-api-contract` into `scripts/run-release-gate-suite.mjs` as gate id `canonical-contract` and included it in default `RELEASE_GATES`; added `release:gate:canonical-contract` npm command for isolated execution; updated `docs/CANONICAL_MODULE_API_CUTOVER_RUNBOOK.md` and `docs/LAUNCH_CHECKLIST.md` so pre-cutover release automation explicitly enforces canonical contract validation.
- `2026-04-22` - **Business OS phase 15 (post-cutover legacy-leak guard automation)** - Added `scripts/check-canonical-module-api-post-cutover.mjs` with npm command `check:canonical-module-api-post-cutover` to fail if legacy response fields appear outside `includeLegacy` guarded blocks in canonicalized module/industry/AI routes; integrated this as release-gate id `canonical-post-cutover` (default-included) in `scripts/run-release-gate-suite.mjs`, added isolated command `release:gate:canonical-post-cutover`, and updated cutover/launch/consumer-readiness docs with command + artifact expectations.
- `2026-04-23` - **Business OS phase 16 (consolidated canonical readiness verdict gate)** - Added `scripts/check-canonical-module-api-readiness-verdict.mjs` to aggregate `check:canonical-module-api-contract`, `check:canonical-module-api-post-cutover`, and `docs/CANONICAL_MODULE_API_CONSUMER_READINESS_CHECKLIST.md` unchecked-item status into one pass/fail verdict artifact; added npm commands `check:canonical-module-api-readiness-verdict` and `release:gate:canonical-readiness-verdict`; registered release-gate id `canonical-readiness-verdict` in `scripts/run-release-gate-suite.mjs`; updated cutover/launch/readiness docs with consolidated gate command and artifact references.
- `2026-04-23` - **Business OS phase 17 (readiness checklist burn-down start)** - Closed evidence-backed readiness checklist items for `check:canonical-module-api-contract` and `check:canonical-module-api-post-cutover` in `docs/CANONICAL_MODULE_API_CONSUMER_READINESS_CHECKLIST.md` based on generated closure artifacts, then re-ran consolidated readiness verdict to refresh remaining unchecked-item count for next execution wave.
- `2026-04-23` - **Business OS phase 18 (response snapshot evidence + checklist burn-down)** - Added `scripts/check-canonical-module-api-response-snapshots.mjs` and npm command `check:canonical-module-api-response-snapshots` to generate explicit legacy-vs-canonical response snapshot artifacts; included this command in consolidated readiness verdict checks and linked it in cutover/launch runbooks; marked readiness checklist item `Response-contract snapshots captured for both flag modes` as complete based on generated evidence artifacts.
- `2026-04-23` - **Business OS phase 19 (consumer field-usage assertion + canonical consumer migration)** - Added `scripts/check-canonical-module-api-consumer-usage.mjs` and npm command `check:canonical-module-api-consumer-usage` to assert canonical endpoint consumers avoid legacy response aliases and reference canonical fields; wired this command into consolidated readiness verdict + launch/cutover docs; migrated consumer usage in `apps/dashboard/app/dashboard/modules/page.tsx`, `apps/dashboard/app/industries/[industry]/page.tsx`, and `apps/dashboard/app/signup/page.tsx` from legacy fields (`recommended/base/coreModules`) to canonical payload access (`canonical.*`, `suites/capabilities`) and marked matching checklist items complete.
- `2026-04-23` - **Business OS phase 20 (prefilled cutover execution worksheet for manual signoff items)** - Added `docs/CANONICAL_MODULE_API_CUTOVER_EXECUTION_WORKSHEET.md` with prefilled owner fields and structured sections for staging validation evidence, production enablement approval/rollback metadata, and 24h post-enable monitoring capture; linked this worksheet in `docs/CANONICAL_MODULE_API_CUTOVER_RUNBOOK.md` and `docs/LAUNCH_CHECKLIST.md` so remaining manual readiness items (staging pass, production approval, post-enable monitoring) can be closed with consistent evidence.
- `2026-04-23` - **Business OS phase 21 (execution pass 1 against cutover worksheet)** - Executed consolidated command bundle via `check:canonical-module-api-readiness-verdict` and captured run output in `docs/evidence/closure/2026-04-23-canonical-cutover-execution-pass-1.md`; updated `docs/CANONICAL_MODULE_API_CUTOVER_EXECUTION_WORKSHEET.md` Pass 1 fields (run date, evidence link, and command-evidence checkboxes) with verified pass states for contract/post-cutover/snapshot/consumer checks while leaving staging runtime validation, production approval, and post-enable monitoring items explicitly pending.
- `2026-04-23` - **Business OS phase 22 (staging runtime evidence sheet for manual closure acceleration)** - Added `docs/CANONICAL_MODULE_API_STAGING_RUNTIME_EVIDENCE_SHEET.md` with prefilled S1-S5 endpoint contract checks, U1-U6 runtime UX flow checks, E1-E3 error-handling checks, and staging decision block; linked this sheet from `docs/CANONICAL_MODULE_API_CUTOVER_EXECUTION_WORKSHEET.md`, `docs/CANONICAL_MODULE_API_CUTOVER_RUNBOOK.md`, and `docs/LAUNCH_CHECKLIST.md` to enable one-pass staging evidence capture and faster closure of remaining manual readiness items.
- `2026-04-23` - **Business OS phase 23 (fill-ready dated staging evidence artifact prewired)** - Added fill-ready artifact `docs/evidence/closure/2026-04-23-canonical-staging-runtime-evidence.md` mirroring the staging runtime evidence sheet with today’s date and prefilled metadata, and prewired this artifact path into `docs/CANONICAL_MODULE_API_CUTOVER_EXECUTION_WORKSHEET.md` so staging outcomes can be pasted directly for faster checklist closure.
- `2026-04-23` - **Business OS phase 24 (staging runtime pre-pass automation script + blocked artifact capture)** - Added `scripts/run-canonical-staging-runtime-checks.mjs` and npm command `check:canonical-staging-runtime` to automate S1-S5 staging contract checks when staging env vars are provided (`CANONICAL_STAGING_BASE_URL`, `CANONICAL_STAGING_AUTH_TOKEN`, optional mutation/industry controls); wired command guidance into staging sheet/worksheet/launch checklist and captured a blocked-run artifact (`docs/evidence/closure/2026-04-23T09-37-09-616Z-canonical-staging-runtime-checks.json`) showing missing env prerequisites in this environment.
- `2026-04-23` - **Business OS phase 25 (staging evidence autofill from runtime artifact)** - Added `scripts/apply-canonical-staging-runtime-artifact.mjs` and npm command `apply:canonical-staging-runtime-artifact` to auto-inject runtime artifact output into the dated staging evidence file (`docs/evidence/closure/2026-04-23-canonical-staging-runtime-evidence.md`), including endpoint-row status, base URL, source artifact pointer, and automation summary; executed once against the blocked artifact so missing env prerequisites are explicitly documented for handoff.
- `2026-04-23` - **Business OS phase 26 (one-command staging evidence pipeline + env template)** - Added `scripts/run-canonical-staging-evidence-pipeline.mjs` and npm command `run:canonical-staging-evidence-pipeline` to chain runtime check + evidence autofill in one step; added `docs/CANONICAL_STAGING_ENV_TEMPLATE.md` with copy/paste PowerShell env setup and linked it in staging sheet/worksheet/launch checklist for faster handoff-to-execution.
- `2026-04-23` - **Business OS phase 27 (canonical details handoff + token fetch helper)** - Expanded `docs/CANONICAL_STAGING_ENV_TEMPLATE.md` with canonical flag/endpoint details and prefilled hosted URL (`https://payaid-v3.vercel.app`) sourced from existing evidence; added `scripts/get-canonical-staging-token.mjs` + npm command `get:canonical-staging-token` to fetch bearer token from `/api/auth/login` using env credentials and print ready-to-paste PowerShell exports; linked token helper in `docs/LAUNCH_CHECKLIST.md` for faster staging execution.
- `2026-04-23` - **Business OS phase 28 (live staging pre-pass executed with real token; canonical flag still not active)** - Ran staging token flow + runtime check pipeline against `https://payaid-v3.vercel.app` and captured executed artifact `docs/evidence/closure/2026-04-23T10-04-35-509Z-canonical-staging-runtime-checks.json`; S1-S5 all returned `200` but still included legacy fields (`compatibility`, `coreModules`, `enabled*`, top-level `recommended/base/industry`), indicating `CANONICAL_MODULE_API_ONLY=1` is not active in the target environment yet; updated fill-ready staging evidence file via autofill script to reflect fail-state endpoint keys and blocker context.
- `2026-04-23` - **Business OS phase 29 (runtime artifact auto-selection hardening)** - Updated `scripts/apply-canonical-staging-runtime-artifact.mjs` to auto-select the latest `*-canonical-staging-runtime-checks.json` artifact from `docs/evidence/closure/` when no explicit artifact path is provided, preventing stale evidence injection from fixed default paths during repeated staging runs.
- `2026-04-23` - **Public CTA copy consistency micro-pass completed** - Standardized the remaining outlier CTA label in `apps/dashboard/app/about/page.tsx` from "Choose Industry & Modules" to the canonical phrase "Choose Industry & Start" so home/pricing/modules/about now use one primary public onboarding CTA wording.
- `2026-04-23` - **Public CTA hierarchy consistency sweep completed** - Promoted the `apps/dashboard/app/modules/page.tsx` header onboarding action to a true primary button style (filled gradient pill) while keeping the same target route, so primary onboarding prominence is now consistent with home/pricing/about.
- `2026-04-23` - **Business OS phase 30 (staging flag root-cause isolated + env-mode hardening fix)** - Verified Vercel `CANONICAL_MODULE_API_ONLY` values are currently stored as `"1\r\n"` when added via stdin (`vercel env add`), which causes strict equality checks to miss canonical-only mode in deployed APIs; hardened `lib/taxonomy/canonical-api-mode.ts` to trim env values before comparison and added regression coverage in `lib/taxonomy/__tests__/canonical-api-mode.test.ts` (`'1\r\n'` case) so canonical-only mode remains reliable across deployment tooling quirks.
- `2026-04-24` - **Marketing IA canonical route map documented** - Added `docs/MARKETING_ROUTE_CANONICAL_MAP.md` defining canonical Marketing routes (`Compose/History/Channels`), current legacy alias redirects, one-time legacy redirect banner behavior, and guardrails to avoid route duplication regressions.
- `2026-04-24` - **Vercel QA handoff aligned to canonical Marketing IA** - Updated `docs/VERCEL_PRODUCTION_TESTING_HANDOFF.md` with Step 4.2 requiring canonical Marketing URLs (`Studio/Campaigns/Social-Media`), redirect verification for legacy bookmarks, and a direct reference to `docs/MARKETING_ROUTE_CANONICAL_MAP.md` to keep production testing consistent with the cleaned route model.
- `2026-04-24` - **Marketing History path canonicalized to `/History`** - Added `apps/dashboard/app/marketing/[tenantId]/History/page.tsx` as canonical History surface, moved History UI into shared `components/marketing/MarketingHistoryPage.tsx`, redirected `Campaigns/page.tsx` to `/History` for backward compatibility, and updated primary Marketing nav/entry links plus docs (`MARKETING_ROUTE_CANONICAL_MAP.md`, `VERCEL_PRODUCTION_TESTING_HANDOFF.md`) to use `Studio/History/Social-Media` canonical URLs.
- `2026-04-24` - **Marketing retry traceability hardening (actor labels + QA step)** - Enhanced `GET /api/social/posts/retries` to resolve `changedBy` user IDs into readable actor labels (`name/email` fallback), updated History retry chips to show actor labels and carry drill-down to Compose audit, and extended `docs/VERCEL_PRODUCTION_TESTING_HANDOFF.md` with Step 4.3 for social retry verification in canonical History flow.
- `2026-04-24` - **Marketing History operations hardening (server filters/pagination/exports + QA)** - Added server-side `from/to` filtering + `offset/total` pagination support in `GET /api/social/posts`, stabilized History filter options, URL-synced filter/page/pageSize state, quick date presets, and CSV export actions (`Export page CSV`, `Export filtered CSV` via `/api/social/posts/export`); expanded `docs/VERCEL_PRODUCTION_TESTING_HANDOFF.md` with Step 4.4 verification coverage.
- `2026-04-24` - **Marketing History server-sort control added** - Added `sort` query support to `GET /api/social/posts` (`created_desc`/`created_asc`), wired History UI sort selector with URL persistence and page reset behavior, and expanded Step 4.4 QA guidance to include sort-order verification.
- `2026-04-24` - **Marketing History shareability polish (result summary + copy link)** - Added matched-row summary (`Showing X-Y of Z`) and `Copy share link` action in History social panel to speed triage collaboration, and expanded Step 4.4 QA checks to validate summary updates and share-link roundtrip behavior.
- `2026-04-24` - **Marketing History resilience polish (page clamp + empty-state clarity)** - Added auto-clamp for out-of-range page values after filter/sort/page-size changes and differentiated empty states (`no data` vs `no matches for current filters`) in History social table; expanded Step 4.4 QA checks for page-clamping and filter-empty messaging behavior.
- `2026-04-24` - **Marketing History export parity polish (sort-aware filtered export)** - Updated `/api/social/posts/export` to honor `sort` (`created_desc`/`created_asc`) and wired History `Export filtered CSV` to pass active sort state so exported datasets align with on-screen triage order; expanded Step 4.4 export verification wording accordingly.
- `2026-04-24` - **Marketing YouTube connector dispatch wiring (preflight + worker runtime)** - Replaced YOUTUBE hard-block preflight with active OAuth readiness checks (`provider in youtube/google`, token expiry, `youtube.upload`/`youtube.force-ssl` scope guidance) in `POST /api/social/posts`; added YouTube worker dispatch in `server/workers/social-post-dispatch.ts` using resumable upload flow (video asset fetch -> upload init -> PUT media -> videoId capture), persisted dispatch metadata (`youtubeDispatch`), and added Step 4.5 runtime QA verification in `docs/VERCEL_PRODUCTION_TESTING_HANDOFF.md`.
- `2026-04-24` - **Social settings YouTube provider parity pass** - Extended social settings API routes to include `youtube` in connect/test/list/disconnect/refresh schemas (`/api/settings/social/*`), added YouTube connection test using YouTube Data API channel probe, and updated Vercel testing handoff Step 4 guidance to explicitly validate YouTube provider visibility and lifecycle actions.
- `2026-04-24` - **YouTube settings UX hardening (health + refresh guidance)** - Added provider health derivation in `/api/settings/social` (including `missing_scope` for YouTube tokens without upload permissions) and upgraded `/api/settings/social/refresh` 501 responses with actionable reconnect guidance + stable error code (`SOCIAL_REFRESH_NOT_IMPLEMENTED`); expanded Vercel handoff Step 4 checks for this behavior.
- `2026-04-24` - **Social settings UI parity for health states + YouTube actions** - Updated settings UI at `settings/[tenantId]/Integrations/Social` to include YouTube manual token connect flow, render backend health badges (`Healthy`, `Expiring soon`, `Expired`, `Missing scope`), show targeted YouTube missing-scope helper copy, and allow refresh action for LinkedIn/YouTube with improved backend guidance surfaced in banner messaging.
- `2026-04-24` - **YouTube provider alias compatibility hardening (`google` -> `youtube`)** - Updated social settings APIs (`/api/settings/social`, `/test`, `/disconnect`, `/refresh`, `/connect`) to normalize and treat legacy `google` OAuth provider entries as canonical YouTube for status, health derivation, test/disconnect/refresh actions, audit entity IDs, and response payload provider names, preventing split-state behavior across historical token storage formats.
- `2026-04-24` - **Social provider alias utility centralization** - Added shared alias helper `lib/integrations/social-provider-aliases.ts` (`normalizeSocialProviderAlias`, `getSocialProviderAliases`, canonical provider lists) and refactored social settings routes (`/api/settings/social/{connect,test,disconnect,refresh}` + list route) to use centralized mapping instead of route-local `google`/`youtube` conditionals, reducing drift risk and keeping provider normalization behavior consistent.
- `2026-04-24` - **Marketing History bulk-retry execution flow added** - Added `POST /api/social/posts/retry-bulk` for tenant-scoped batch retry of failed `MarketingPost` rows (status guards, audit events, queue re-enqueue, retried/skipped summary) and wired History UI multi-select actions (`Select all failed on page`, `Retry selected (N)`) with post-action refresh and status-filter fallback for fast incident recovery.
- `2026-04-24` - **YouTube E2E smoke acceptance criteria hardening** - Expanded `docs/VERCEL_PRODUCTION_TESTING_HANDOFF.md` Step 4.5 with explicit expected runtime markers for settings test API pass, social post creation/preflight responses, worker status transitions (`SCHEDULED -> SENT/FAILED`), required `youtubeDispatch` metadata keys (`videoId`, `videoUrl`, `postedAt`), and History/Open-audit verification points for deterministic QA signoff.
- `2026-04-24` - **Sales Pages foundation audit + architecture recommendation documented** - Added `docs/SALES_PAGES_MODULE_FOUNDATION_RECOMMENDATION.md` with current-state audit of existing Sales `Landing-Pages`/`Checkout-Pages` implementation, OSS comparison (Destack-first decision), canonical `sales_*` entity proposal, API/event model, CRM sync + automation rules, integration mapping (CRM/Marketing/Appointments/Finance/Voice), implementation risks, phased rollout plan, and MVP QA acceptance checklist.
- `2026-04-24` - **Sales Pages execution scaffolding (schema draft + canonical bridge APIs + screen plan)** - Added canonical contract constants (`apps/dashboard/lib/sales-pages/contracts.ts`), new bridge API namespace (`apps/dashboard/app/api/sales-pages/*`) with list/create/detail/update/publish/unpublish handlers, submission ingest stub (`apps/dashboard/app/api/sales-submissions/route.ts`), canonical schema draft file (`prisma/proposals/sales-pages-canonical-schema.prisma`), and execution-ready per-screen implementation tracker (`docs/SALES_PAGES_SCREEN_IMPLEMENTATION_PLAN.md`).
- `2026-04-24` - **Sales Pages UI bridge wiring + migration dry-run script scaffold** - Rewired Sales Landing Pages screens to canonical bridge endpoints (`/api/sales-pages`) in list/new/detail flows, added list-level status/type filters plus publish/unpublish actions in `apps/dashboard/app/sales/[tenantId]/Landing-Pages/page.tsx`, and added migration skeleton `scripts/migrations/migrate-landing-pages-to-sales-pages.mjs` with npm script `migrate:sales-pages:dry-run` to export LandingPage -> canonical sales_page transformed payload artifacts under `docs/evidence/migrations/`.
- `2026-04-24` - **Sales Pages canonical route surfaced as primary IA path** - Added canonical route family `apps/dashboard/app/sales/[tenantId]/Sales-Pages/*` as wrappers over current implementation for backward compatibility, switched Sales module top-nav layouts (`Home`, `Checkout-Pages`, `Landing-Pages`, `Orders`) to point at `/sales/[tenantId]/Sales-Pages`, and updated Sales/Home + Checkout-page local nav links/CTAs to use the canonical `Sales-Pages` paths while preserving old `Landing-Pages` routes.
- `2026-04-24` - **Legacy Landing-Pages routes now auto-redirect to canonical Sales-Pages** - Added in-component route replacement guards to `apps/dashboard/app/sales/[tenantId]/Landing-Pages/page.tsx`, `.../Landing-Pages/new/page.tsx`, and `.../Landing-Pages/[id]/page.tsx` so direct legacy URL hits are immediately redirected to `/sales/[tenantId]/Sales-Pages*` while existing canonical screen rendering remains unchanged.
- `2026-04-24` - **Server-side Sales Pages canonical redirect enforced in proxy** - Updated `apps/dashboard/proxy.ts` to redirect any request path containing `/Landing-Pages` to `/Sales-Pages` using `NextResponse.redirect(...)`, preserving query params and ensuring canonical Sales Pages URLs resolve before client hydration.
- `2026-04-24` - **Legacy client-side redirect guards removed after proxy canonicalization** - Removed now-redundant `usePathname` + `router.replace(...)` fallback redirect logic from `apps/dashboard/app/sales/[tenantId]/Landing-Pages/page.tsx`, `.../new/page.tsx`, and `.../[id]/page.tsx`; canonicalization is now handled centrally by `apps/dashboard/proxy.ts`.
- `2026-04-24` - **Sales canonical route map + QA handoff alignment added** - Added `docs/SALES_ROUTE_CANONICAL_MAP.md` (canonical Sales IA + legacy redirect mapping + guardrails) and updated `docs/VERCEL_PRODUCTION_TESTING_HANDOFF.md` with Step 4.7 requiring canonical Sales routes and explicit `Landing-Pages` -> `Sales-Pages` redirect verification.
- `2026-04-24` - **Sales proxy canonical redirect regression test added** - Added `apps/dashboard/__tests__/proxy.test.ts` to assert proxy-level redirect behavior for legacy `Landing-Pages` routes (list + detail/query preservation) and non-redirect behavior for canonical `Sales-Pages` routes. Focused Jest run attempt for this path hung in this shell session and was force-terminated; keep test in CI suite for authoritative execution.
- `2026-04-24` - **Dedicated Sales canonical proxy test runner added** - Added npm command `test:sales:proxy-canonical` in `package.json` to run `apps/dashboard/__tests__/proxy.test.ts` directly, and documented the runner in `docs/SALES_ROUTE_CANONICAL_MAP.md` under redirect enforcement notes. Local command execution was attempted and reproduced the existing Jest hang behavior in this shell; process was terminated and the script remains ready for CI/stable environments.
- `2026-04-24` - **Marketing History filtered-failed bulk retry flow added** - Extended `POST /api/social/posts/retry-bulk` with `mode=filtered_failed` (tenant-scoped, optional channel/date filters, capped batch selection) and wired `components/marketing/MarketingHistoryPage.tsx` with `Retry all failed (filtered)` action so operators can requeue failed rows across the active filter window in one click instead of page-by-page selection.
- `2026-04-24` - **Compose channel-readiness strip + fix links added** - Updated `components/marketing/MarketingStudioForm.tsx` to fetch `/api/settings/social` connector health for selected social channels, surface pre-launch readiness blockers/warnings (`not_connected`, `expired`, `missing_scope`, `expiring_soon`) in a dedicated `Channel readiness` strip, add one-click fix links to `settings/[tenantId]/Integrations/Social` and `marketing/[tenantId]/Social-Media`, and block `Schedule social` when readiness errors remain.
- `2026-04-24` - **History failure analytics (7d/30d top reasons) added** - Added `GET /api/social/posts/failure-analytics` to aggregate failed `MarketingPost` rows into normalized failure categories + top raw reason strings (with channel-scoped filtering), and wired `components/marketing/MarketingHistoryPage.tsx` with a `Top failure reasons` panel (`7d`/`30d` toggle, total failed count, category chips, and top reason list) for faster retry triage.
- `2026-04-24` - **History CSV export audit metadata rows added** - Updated `components/marketing/MarketingHistoryPage.tsx` `Export page CSV` and `GET /api/social/posts/export` (`apps/dashboard/app/api/social/posts/export/route.ts`) to prepend machine-readable `meta` rows (exportedAt, active filters, sort, and window/page limits) so exported files are self-describing for audit/replay workflows.
- `2026-04-24` - **Marketing Step 4 local execution pass-1 evidence captured** - Ran `npm run verify:db` (pass with non-blocking env warnings) and documented Step 4.1-4.5 local execution status plus blocked/hung checks in `docs/evidence/closure/2026-04-24-marketing-step4-local-execution-pass-1.md`; marked authenticated browser/OAuth-dependent assertions as pending staging/prod verification.
- `2026-04-24` - **Marketing Step 4 authenticated QA template prepared** - Added fill-ready execution worksheet `docs/evidence/closure/2026-04-24-marketing-step4-authenticated-qa-template.md` (Step 4.1-4.5 status blocks, checklist items, evidence slots, and consolidated release verdict) and linked it from `docs/VERCEL_PRODUCTION_TESTING_HANDOFF.md` for faster, standardized QA capture.
- `2026-04-24` - **AI Website Builder foundation + module spec documented** - Added `docs/AI_WEBSITE_BUILDER_FOUNDATION_AND_MODULE_SPEC.md` with OSS shortlist and recommendation (`Craft.js`/`GrapesJS` foundation), architecture layers, canonical `website_*` schema proposal, API/event model, CRM sync and automation rules, publish/domain guardrails, implementation risks, phased rollout plan, and MVP acceptance checklist.
- `2026-04-24` - **AI Website Builder execution scaffolding added** - Added canonical contracts scaffold `apps/dashboard/lib/website-builder/contracts.ts`, bridge API routes under `apps/dashboard/app/api/website/*` (`sites` list/create/detail/update/publish/unpublish, `submissions`, and `ai/generate-draft`), canonical schema draft `prisma/proposals/website-builder-canonical-schema.prisma`, and per-screen execution tracker `docs/WEBSITE_BUILDER_SCREEN_IMPLEMENTATION_PLAN.md`.
- `2026-04-24` - **AI Website Builder canonical-mode repository + migration dry-run script added** - Added canonical mode flag helper `lib/website-builder/canonical-api-mode.ts` (`CANONICAL_WEBSITE_BUILDER_API_ONLY`), centralized data-access switcher `lib/website-builder/repository.ts` (canonical `website_*` when available, bridge fallback to `LandingPage`), rewired `apps/dashboard/app/api/website/*` routes to use the repository, added migration transform script `scripts/migrations/migrate-landing-pages-to-website-builder.mjs`, and added npm command `migrate:website-builder:dry-run` in `package.json`.
- `2026-04-24` - **Website Builder Home wired to live APIs (Phase 3 kickoff)** - Upgraded `apps/dashboard/app/website-builder/[tenantId]/Home/page.tsx` from static placeholder to a working MVP shell with authenticated list fetch (`GET /api/website/sites`), status/goal filters, inline `Create Website` form (`POST /api/website/sites`), and publish/unpublish actions (`POST /api/website/sites/:id/publish|unpublish`) using TanStack Query mutations and refresh flows.
- `2026-04-24` - **Website Builder site detail/edit page added and Home linked** - Added `apps/dashboard/app/website-builder/[tenantId]/Sites/[id]/page.tsx` with authenticated `GET/PATCH /api/website/sites/:id` wiring for editable site metadata (name, slug, status, goal, SEO fields), and updated Home cards in `apps/dashboard/app/website-builder/[tenantId]/Home/page.tsx` to route `Open Site` into the new detail editor path.
- `2026-04-24` - **Website Builder draft-generation + read-only page-tree panel wired** - Extended `apps/dashboard/app/website-builder/[tenantId]/Sites/[id]/page.tsx` with `Generate Draft` action that calls `POST /api/website/ai/generate-draft` and persists returned draft data via `PATCH /api/website/sites/:id` (`schemaJson.aiDraft`), plus a read-only page-tree panel that renders from stored `pageTree` or fallback AI draft page plan.
- `2026-04-24` - **Website Builder Apply-Draft-to-Pages flow added** - Extended `PATCH /api/website/sites/:id` contract to accept `pageTree`, updated `lib/website-builder/repository.ts` bridge persistence to store `contentJson.pages`, and added `Apply Draft to Pages` action in `apps/dashboard/app/website-builder/[tenantId]/Sites/[id]/page.tsx` to map `schemaJson.aiDraft.pagePlan` into persisted page-tree entries (slug/title/pageType/order/sections) with `aiDraftAppliedAt` marker.
- `2026-04-24` - **Website Builder lightweight page-tree editor added** - Extended `apps/dashboard/app/website-builder/[tenantId]/Sites/[id]/page.tsx` with inline page-tree editing controls (rename slug/title/type, move up/down, delete, add page) and `Save Page Tree` persistence via `PATCH /api/website/sites/:id` (`pageTree` payload), enabling manual page-structure adjustments after AI draft application.
- `2026-04-24` - **Website Builder page-tree save guardrails added** - Added client-side page-tree validation in `apps/dashboard/app/website-builder/[tenantId]/Sites/[id]/page.tsx` before save (minimum one page, non-empty title/slug per row, case-insensitive unique slugs) with inline error panel and blocked save until issues are resolved.
- `2026-04-24` - **Website Builder server-side page-tree validation added** - Added mirror validation in `apps/dashboard/app/api/website/sites/[id]/route.ts` for `PATCH` `pageTree` payloads (minimum one page, required title/slug, case-insensitive unique slugs) and return `400` with detailed validation messages when invalid, so non-UI clients cannot bypass page-tree integrity rules.
- `2026-04-24` - **Website Builder page-tree validation API tests added** - Added focused route tests in `apps/dashboard/app/api/website/sites/[id]/__tests__/route.test.ts` covering empty page tree, duplicate slug (case-insensitive), missing title/slug, and valid payload acceptance for `PATCH /api/website/sites/:id`; local Jest invocation for this path hung in the current shell session, so static review + lint pass completed and rerun is pending on a healthy test shell.
- `2026-04-24` - **Website Builder shared page-tree validator + unit tests added** - Added shared helper `lib/website-builder/page-tree-validation.ts` and unit tests `lib/website-builder/__tests__/page-tree-validation.test.ts` for empty tree, missing title/slug, case-insensitive duplicate slug, and valid payload; refactored both UI (`apps/dashboard/app/website-builder/[tenantId]/Sites/[id]/page.tsx`) and API (`apps/dashboard/app/api/website/sites/[id]/route.ts`) to use the same validator for consistent client/server behavior.
- `2026-04-24` - **Website Builder API validation details contract locked** - Extended `apps/dashboard/app/api/website/sites/[id]/__tests__/route.test.ts` with an explicit ordered-`details` assertion for mixed page-tree validation failures, ensuring stable error message ordering/format for frontend inline error rendering expectations.
